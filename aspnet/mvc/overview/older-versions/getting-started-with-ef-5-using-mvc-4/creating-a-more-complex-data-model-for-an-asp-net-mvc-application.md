---
uid: mvc/overview/older-versions/getting-started-with-ef-5-using-mvc-4/creating-a-more-complex-data-model-for-an-asp-net-mvc-application
title: 为 ASP.NET MVC 应用程序 (4 个) 创建更复杂的数据模型 |Microsoft Docs
author: tdykstra
description: Contoso 大学示例 web 应用程序演示如何使用实体框架 5 Code First 和 Visual Studio .。。
ms.author: riande
ms.date: 07/30/2013
ms.assetid: f81f3d80-3674-4d8e-a9b1-87feed1a93c9
msc.legacyurl: /mvc/overview/older-versions/getting-started-with-ef-5-using-mvc-4/creating-a-more-complex-data-model-for-an-asp-net-mvc-application
msc.type: authoredcontent
ms.openlocfilehash: 84e1218d29dd4d4afcf2b876c027b0579f012954
ms.sourcegitcommit: b4cdcf246850751579e45da80c9780fe56330dd0
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 02/09/2021
ms.locfileid: "99985484"
---
# <a name="creating-a-more-complex-data-model-for-an-aspnet-mvc-application-4-of-10"></a><span data-ttu-id="e39dd-103">为 ASP.NET MVC 应用程序创建更复杂的数据模型 (4 的 10) </span><span class="sxs-lookup"><span data-stu-id="e39dd-103">Creating a More Complex Data Model for an ASP.NET MVC Application (4 of 10)</span></span>

<span data-ttu-id="e39dd-104">作者： [Tom Dykstra](https://github.com/tdykstra)</span><span class="sxs-lookup"><span data-stu-id="e39dd-104">by [Tom Dykstra](https://github.com/tdykstra)</span></span>

> <span data-ttu-id="e39dd-105">Contoso 大学示例 web 应用程序演示了如何使用实体框架 5 Code First 和 Visual Studio 2012 创建 ASP.NET MVC 4 应用程序。</span><span class="sxs-lookup"><span data-stu-id="e39dd-105">The Contoso University sample web application demonstrates how to create ASP.NET MVC 4 applications using the Entity Framework 5 Code First and Visual Studio 2012.</span></span> <span data-ttu-id="e39dd-106">若要了解教程系列，请参阅[本系列中的第一个教程](creating-an-entity-framework-data-model-for-an-asp-net-mvc-application.md)。</span><span class="sxs-lookup"><span data-stu-id="e39dd-106">For information about the tutorial series, see [the first tutorial in the series](creating-an-entity-framework-data-model-for-an-asp-net-mvc-application.md).</span></span>
> 
> > [!NOTE] 
> > 
> > <span data-ttu-id="e39dd-107">如果遇到无法解决的问题，请 [下载已完成的章节](building-the-ef5-mvc4-chapter-downloads.md) 并尝试重现你的问题。</span><span class="sxs-lookup"><span data-stu-id="e39dd-107">If you run into a problem you can't resolve, [download the completed chapter](building-the-ef5-mvc4-chapter-downloads.md) and try to reproduce your problem.</span></span> <span data-ttu-id="e39dd-108">通常可以通过将代码与已完成的代码进行比较，查找问题的解决方案。</span><span class="sxs-lookup"><span data-stu-id="e39dd-108">You can generally find the solution to the problem by comparing your code to the completed code.</span></span> <span data-ttu-id="e39dd-109">有关一些常见错误以及如何解决这些错误，请参阅 [错误和解决方法。](advanced-entity-framework-scenarios-for-an-mvc-web-application.md#errors)</span><span class="sxs-lookup"><span data-stu-id="e39dd-109">For some common errors and how to solve them, see [Errors and Workarounds.](advanced-entity-framework-scenarios-for-an-mvc-web-application.md#errors)</span></span>

<span data-ttu-id="e39dd-110">在前面的教程中，你使用了由三个实体组成的简单数据模型。</span><span class="sxs-lookup"><span data-stu-id="e39dd-110">In the previous tutorials you worked with a simple data model that was composed of three entities.</span></span> <span data-ttu-id="e39dd-111">在本教程中，您将添加更多实体和关系，并通过指定格式设置、验证和数据库映射规则来自定义数据模型。</span><span class="sxs-lookup"><span data-stu-id="e39dd-111">In this tutorial you'll add more entities and relationships and you'll customize the data model by specifying formatting, validation, and database mapping rules.</span></span> <span data-ttu-id="e39dd-112">你将看到两种自定义数据模型的方式：通过将特性添加到实体类，并将代码添加到数据库上下文类中。</span><span class="sxs-lookup"><span data-stu-id="e39dd-112">You'll see two ways to customize the data model: by adding attributes to entity classes and by adding code to the database context class.</span></span>

<span data-ttu-id="e39dd-113">完成本教程后，实体类将构成下图所示的完整数据模型：</span><span class="sxs-lookup"><span data-stu-id="e39dd-113">When you're finished, the entity classes will make up the completed data model that's shown in the following illustration:</span></span>

![School_class_diagram](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image1.png)

## <a name="customize-the-data-model-by-using-attributes"></a><span data-ttu-id="e39dd-115">使用特性自定义数据模型</span><span class="sxs-lookup"><span data-stu-id="e39dd-115">Customize the Data Model by Using Attributes</span></span>

<span data-ttu-id="e39dd-116">本节介绍如何使用指定格式化、验证和数据库映射规则的特性来自定义数据模型。</span><span class="sxs-lookup"><span data-stu-id="e39dd-116">In this section you'll see how to customize the data model by using attributes that specify formatting, validation, and database mapping rules.</span></span> <span data-ttu-id="e39dd-117">然后，在以下几节中，您将 `School` 通过向已创建的类添加特性并为模型中的其余实体类型创建新类，来创建完整的数据模型。</span><span class="sxs-lookup"><span data-stu-id="e39dd-117">Then in several of the following sections you'll create the complete `School` data model by adding attributes to the classes you already created and creating new classes for the remaining entity types in the model.</span></span>

### <a name="the-datatype-attribute"></a><span data-ttu-id="e39dd-118">DataType 特性</span><span class="sxs-lookup"><span data-stu-id="e39dd-118">The DataType Attribute</span></span>

<span data-ttu-id="e39dd-119">对于学生注册日期，目前所有网页都显示有时间和日期，尽管对此字段而言重要的只是日期。</span><span class="sxs-lookup"><span data-stu-id="e39dd-119">For student enrollment dates, all of the web pages currently display the time along with the date, although all you care about for this field is the date.</span></span> <span data-ttu-id="e39dd-120">使用数据注释特性，可更改一次代码，修复每个视图中数据的显示格式。</span><span class="sxs-lookup"><span data-stu-id="e39dd-120">By using data annotation attributes, you can make one code change that will fix the display format in every view that shows the data.</span></span> <span data-ttu-id="e39dd-121">若要查看如何执行此操作，请向 `Student` 类的 `EnrollmentDate` 属性添加一个特性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-121">To see an example of how to do that, you'll add an attribute to the `EnrollmentDate` property in the `Student` class.</span></span>

<span data-ttu-id="e39dd-122">在 *Models\Student.cs* 中，为 `using` 命名空间添加一个语句， `System.ComponentModel.DataAnnotations` 并将 `DataType` 和 `DisplayFormat` 属性添加到 `EnrollmentDate` 属性，如以下示例中所示：</span><span class="sxs-lookup"><span data-stu-id="e39dd-122">In *Models\Student.cs*, add a `using` statement for the `System.ComponentModel.DataAnnotations` namespace and add `DataType` and `DisplayFormat` attributes to the `EnrollmentDate` property, as shown in the following example:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample1.cs?highlight=3,13-14)]

<span data-ttu-id="e39dd-123">[DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx)特性用于指定比数据库内部类型更具体的数据类型。</span><span class="sxs-lookup"><span data-stu-id="e39dd-123">The [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attribute is used to specify a data type that is more specific than the database intrinsic type.</span></span> <span data-ttu-id="e39dd-124">在此示例中，我们只想跟踪日期，而不是日期和时间。</span><span class="sxs-lookup"><span data-stu-id="e39dd-124">In this case we only want to keep track of the date, not the date and time.</span></span> <span data-ttu-id="e39dd-125">[DataType 枚举](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx)提供多种数据类型，例如 *日期、时间、PhoneNumber、货币、EmailAddress* 等。</span><span class="sxs-lookup"><span data-stu-id="e39dd-125">The [DataType Enumeration](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) provides for many data types, such as *Date, Time, PhoneNumber, Currency, EmailAddress* and more.</span></span> <span data-ttu-id="e39dd-126">应用程序还可通过 `DataType` 特性自动提供类型特定的功能。</span><span class="sxs-lookup"><span data-stu-id="e39dd-126">The `DataType` attribute can also enable the application to automatically provide type-specific features.</span></span> <span data-ttu-id="e39dd-127">例如， `mailto:` 可以为[EmailAddress](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx)创建链接，并且可以在支持[HTML5](http://html5.org/)的浏览器中为[数据类型](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx)提供日期选择器。</span><span class="sxs-lookup"><span data-stu-id="e39dd-127">For example, a `mailto:` link can be created for [DataType.EmailAddress](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx), and a date selector can be provided for [DataType.Date](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) in browsers that support [HTML5](http://html5.org/).</span></span> <span data-ttu-id="e39dd-128">数据 [类型](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) 属性发出 html 5 [数据](http://ejohn.org/blog/html-5-data-attributes/) *(发音* 为 html 5 浏览器可以理解的) 属性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-128">The [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attributes emits HTML 5 [data-](http://ejohn.org/blog/html-5-data-attributes/) (pronounced *data dash*) attributes that HTML 5 browsers can understand.</span></span> <span data-ttu-id="e39dd-129">[DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx)特性不提供任何验证。</span><span class="sxs-lookup"><span data-stu-id="e39dd-129">The [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attributes do not provide any validation.</span></span>

<span data-ttu-id="e39dd-130">`DataType.Date` 不指定显示日期的格式。</span><span class="sxs-lookup"><span data-stu-id="e39dd-130">`DataType.Date` does not specify the format of the date that is displayed.</span></span> <span data-ttu-id="e39dd-131">默认情况下，数据字段根据服务器的 [CultureInfo](https://msdn.microsoft.com/library/vstudio/system.globalization.cultureinfo(v=vs.110).aspx)按默认格式显示。</span><span class="sxs-lookup"><span data-stu-id="e39dd-131">By default, the data field is displayed according to the default formats based on the server's [CultureInfo](https://msdn.microsoft.com/library/vstudio/system.globalization.cultureinfo(v=vs.110).aspx).</span></span>

<span data-ttu-id="e39dd-132">`DisplayFormat` 特性用于显式指定日期格式：</span><span class="sxs-lookup"><span data-stu-id="e39dd-132">The `DisplayFormat` attribute is used to explicitly specify the date format:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample2.cs)]

<span data-ttu-id="e39dd-133">此 `ApplyFormatInEditMode` 设置指定当在文本框中显示值进行编辑时，还应应用指定的格式设置。</span><span class="sxs-lookup"><span data-stu-id="e39dd-133">The `ApplyFormatInEditMode` setting specifies that the specified formatting should also be applied when the value is displayed in a text box for editing.</span></span> <span data-ttu-id="e39dd-134"> (您可能不希望为某些字段（例如，对于货币值），您可能不希望文本框中的货币符号用于编辑。 ) </span><span class="sxs-lookup"><span data-stu-id="e39dd-134">(You might not want that for some fields — for example, for currency values, you might not want the currency symbol in the text box for editing.)</span></span>

<span data-ttu-id="e39dd-135">您可以单独使用 [DisplayFormat](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) 属性，但通常最好使用 [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) 属性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-135">You can use the [DisplayFormat](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) attribute by itself, but it's generally a good idea to use the [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attribute also.</span></span> <span data-ttu-id="e39dd-136">`DataType`属性传达数据的 *语义*，而不是在屏幕上呈现数据的语义，并提供以下不需要的优点 `DisplayFormat` ：</span><span class="sxs-lookup"><span data-stu-id="e39dd-136">The `DataType` attribute conveys the *semantics* of the data as opposed to how to render it on a screen, and provides the following benefits that you don't get with `DisplayFormat`:</span></span>

- <span data-ttu-id="e39dd-137">浏览器可以启用 HTML5 功能 (例如，显示日历控件、区域设置相应的货币符号、电子邮件链接等 ) 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-137">The browser can enable HTML5 features (for example to show a calendar control, the locale-appropriate currency symbol, email links, etc.).</span></span>
- <span data-ttu-id="e39dd-138">默认情况下，浏览器将根据 [区域设置](https://msdn.microsoft.com/library/vstudio/wyzd2bce.aspx)使用正确的格式呈现数据。</span><span class="sxs-lookup"><span data-stu-id="e39dd-138">By default, the browser will render data using the correct format based on your [locale](https://msdn.microsoft.com/library/vstudio/wyzd2bce.aspx).</span></span>
- <span data-ttu-id="e39dd-139">[DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx)特性可以使 MVC 选择正确的字段模板来呈现数据 (如果[DisplayFormat](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx)使用字符串模板) 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-139">The [DataType](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatypeattribute.aspx) attribute can enable MVC to choose the right field template to render the data (the [DisplayFormat](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) if used by itself uses the string template).</span></span> <span data-ttu-id="e39dd-140">有关详细信息，请参阅 Brad Wilson 的 [ASP.NET MVC 2 模板](http://bradwilson.typepad.com/blog/2009/10/aspnet-mvc-2-templates-part-1-introduction.html)。</span><span class="sxs-lookup"><span data-stu-id="e39dd-140">For more information, see Brad Wilson's [ASP.NET MVC 2 Templates](http://bradwilson.typepad.com/blog/2009/10/aspnet-mvc-2-templates-part-1-introduction.html).</span></span> <span data-ttu-id="e39dd-141">尽管 (是为 MVC 2 编写的，但本文仍适用于当前版本的 ASP.NET MVC。 ) </span><span class="sxs-lookup"><span data-stu-id="e39dd-141">(Though written for MVC 2, this article still applies to the current version of ASP.NET MVC.)</span></span>

<span data-ttu-id="e39dd-142">如果将 `DataType` 属性与日期字段一起使用，则还必须指定属性，以 `DisplayFormat` 确保字段在 Chrome 浏览器中正确呈现。</span><span class="sxs-lookup"><span data-stu-id="e39dd-142">If you use the `DataType` attribute with a date field, you have to specify the `DisplayFormat` attribute also in order to ensure that the field renders correctly in Chrome browsers.</span></span> <span data-ttu-id="e39dd-143">有关详细信息，请参阅 [此 StackOverflow 线程](http://stackoverflow.com/questions/12633471/mvc4-datatype-date-editorfor-wont-display-date-value-in-chrome-fine-in-ie)。</span><span class="sxs-lookup"><span data-stu-id="e39dd-143">For more information, see [this StackOverflow thread](http://stackoverflow.com/questions/12633471/mvc4-datatype-date-editorfor-wont-display-date-value-in-chrome-fine-in-ie).</span></span>

<span data-ttu-id="e39dd-144">再次运行 "学生索引" 页，注意注册日期不再显示时间。</span><span class="sxs-lookup"><span data-stu-id="e39dd-144">Run the Student Index page again and notice that times are no longer displayed for the enrollment dates.</span></span> <span data-ttu-id="e39dd-145">对于使用该模型的任何视图，这一点都是相同的 `Student` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-145">The same will be true for any view that uses the `Student` model.</span></span>

![Students_index_page_with_formatted_date](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image2.png)

### <a name="the-stringlengthattribute"></a><span data-ttu-id="e39dd-147">StringLengthAttribute</span><span class="sxs-lookup"><span data-stu-id="e39dd-147">The StringLengthAttribute</span></span>

<span data-ttu-id="e39dd-148">你还可以使用特性指定数据验证规则和消息。</span><span class="sxs-lookup"><span data-stu-id="e39dd-148">You can also specify data validation rules and messages using attributes.</span></span> <span data-ttu-id="e39dd-149">假设要确保用户输入的名称不超过 50 个字符。</span><span class="sxs-lookup"><span data-stu-id="e39dd-149">Suppose you want to ensure that users don't enter more than 50 characters for a name.</span></span> <span data-ttu-id="e39dd-150">若要添加此限制，请将 [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) 特性添加到 `LastName` 和 `FirstMidName` 属性，如以下示例中所示：</span><span class="sxs-lookup"><span data-stu-id="e39dd-150">To add this limitation, add [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) attributes to the `LastName` and `FirstMidName` properties, as shown in the following example:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample3.cs?highlight=10,12)]

<span data-ttu-id="e39dd-151">[StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx)属性不会阻止用户为某个名称输入空格。</span><span class="sxs-lookup"><span data-stu-id="e39dd-151">The [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) attribute won't prevent a user from entering white space for a name.</span></span> <span data-ttu-id="e39dd-152">可以使用 [RegularExpression](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.regularexpressionattribute.aspx) 属性将限制应用到输入。</span><span class="sxs-lookup"><span data-stu-id="e39dd-152">You can use the [RegularExpression](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.regularexpressionattribute.aspx) attribute to apply restrictions to the input.</span></span> <span data-ttu-id="e39dd-153">例如，下面的代码要求第一个字符为大写，其余的字符为字母顺序：</span><span class="sxs-lookup"><span data-stu-id="e39dd-153">For example the following code requires the first character to be upper case and the remaining characters to be alphabetical:</span></span>

`[RegularExpression(@"^[A-Z]+[a-zA-Z""'\s-]*$")]`

<span data-ttu-id="e39dd-154">[MaxLength](https://msdn.microsoft.com/library/System.ComponentModel.DataAnnotations.MaxLengthAttribute.aspx)特性为[StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx)特性提供了类似的功能，但不提供客户端验证。</span><span class="sxs-lookup"><span data-stu-id="e39dd-154">The [MaxLength](https://msdn.microsoft.com/library/System.ComponentModel.DataAnnotations.MaxLengthAttribute.aspx) attribute provides similar functionality to the [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) attribute but doesn't provide client side validation.</span></span>

<span data-ttu-id="e39dd-155">运行应用程序，然后单击 " **学生** " 选项卡。收到以下错误：</span><span class="sxs-lookup"><span data-stu-id="e39dd-155">Run the application and click the **Students** tab. You get the following error:</span></span>

<span data-ttu-id="e39dd-156">*创建数据库后，支持 "SchoolContext" 上下文的模型已发生更改。请考虑使用 Code First 迁移更新数据库 ([https://go.microsoft.com/fwlink/?LinkId=238269](https://go.microsoft.com/fwlink/?LinkId=238269)) 。*</span><span class="sxs-lookup"><span data-stu-id="e39dd-156">*The model backing the 'SchoolContext' context has changed since the database was created. Consider using Code First Migrations to update the database ([https://go.microsoft.com/fwlink/?LinkId=238269](https://go.microsoft.com/fwlink/?LinkId=238269)).*</span></span>

<span data-ttu-id="e39dd-157">数据库模型已更改，这种情况下需要更改数据库架构，并且实体框架检测到的情况。</span><span class="sxs-lookup"><span data-stu-id="e39dd-157">The database model has changed in a way that requires a change in the database schema, and Entity Framework detected that.</span></span> <span data-ttu-id="e39dd-158">你将使用迁移来更新架构，而不会丢失你使用 UI 添加到数据库的任何数据。</span><span class="sxs-lookup"><span data-stu-id="e39dd-158">You'll use migrations to update the schema without losing any data that you added to the database by using the UI.</span></span> <span data-ttu-id="e39dd-159">如果更改了通过方法创建的数据 `Seed` ，则会将其更改回其原始状态，因为在方法中使用的是 [AddOrUpdate](https://msdn.microsoft.com/library/hh846520(v=vs.103).aspx) 方法 `Seed` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-159">If you changed data that was created by the `Seed` method, that will be changed back to its original state because of the [AddOrUpdate](https://msdn.microsoft.com/library/hh846520(v=vs.103).aspx) method that you're using in the `Seed` method.</span></span> <span data-ttu-id="e39dd-160"> ([AddOrUpdate](https://msdn.microsoft.com/library/hh846520(v=vs.103).aspx) 等效于数据库术语中的 "upsert" 操作。 ) </span><span class="sxs-lookup"><span data-stu-id="e39dd-160">([AddOrUpdate](https://msdn.microsoft.com/library/hh846520(v=vs.103).aspx) is equivalent to an "upsert" operation from database terminology.)</span></span>

<span data-ttu-id="e39dd-161">在包管理器控制台 (PMC) 中输入以下命令：</span><span class="sxs-lookup"><span data-stu-id="e39dd-161">In the Package Manager Console (PMC), enter the following commands:</span></span>

[!code-console[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample4.cmd)]

<span data-ttu-id="e39dd-162">`add-migration MaxLengthOnNames`命令创建一个名为 *&lt; timeStamp &gt; \_ MaxLengthOnNames.cs* 的文件。</span><span class="sxs-lookup"><span data-stu-id="e39dd-162">The `add-migration MaxLengthOnNames` command creates a file named *&lt;timeStamp&gt;\_MaxLengthOnNames.cs*.</span></span> <span data-ttu-id="e39dd-163">此文件包含将更新数据库以匹配当前数据模型的代码。</span><span class="sxs-lookup"><span data-stu-id="e39dd-163">This file contains code that will update the database to match the current data model.</span></span> <span data-ttu-id="e39dd-164">实体框架使用迁移文件名前面预置的时间戳来对迁移进行排序。</span><span class="sxs-lookup"><span data-stu-id="e39dd-164">The timestamp prepended to the migrations file name is used by Entity Framework to order the migrations.</span></span> <span data-ttu-id="e39dd-165">创建多个迁移后，如果删除数据库，或通过使用迁移来部署项目，则将按照创建的顺序应用所有迁移。</span><span class="sxs-lookup"><span data-stu-id="e39dd-165">After you have created multiple migrations, if you drop the database, or if you deploy the project by using Migrations, all of the migrations are applied in the order in which they were created.</span></span>

<span data-ttu-id="e39dd-166">运行 " **创建** " 页，并输入长度超过50个字符的名称。</span><span class="sxs-lookup"><span data-stu-id="e39dd-166">Run the **Create** page, and enter either name longer than 50 characters.</span></span> <span data-ttu-id="e39dd-167">超过50个字符后，客户端验证会立即显示错误消息。</span><span class="sxs-lookup"><span data-stu-id="e39dd-167">As soon as you exceed 50 characters, client side validation immediately shows an error message.</span></span>

![客户端 val 错误](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image3.png)

### <a name="the-column-attribute"></a><span data-ttu-id="e39dd-169">列属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-169">The Column Attribute</span></span>

<span data-ttu-id="e39dd-170">还可使用特性来控制类和属性映射到数据库的方式。</span><span class="sxs-lookup"><span data-stu-id="e39dd-170">You can also use attributes to control how your classes and properties are mapped to the database.</span></span> <span data-ttu-id="e39dd-171">假设在名字字段使用了 `FirstMidName`，这是因为该字段也可能包含中间名。</span><span class="sxs-lookup"><span data-stu-id="e39dd-171">Suppose you had used the name `FirstMidName` for the first-name field because the field might also contain a middle name.</span></span> <span data-ttu-id="e39dd-172">但却希望将数据库列命名为 `FirstName`，因为要针对数据库编写即席查询的用户习惯使用该姓名。</span><span class="sxs-lookup"><span data-stu-id="e39dd-172">But you want the database column to be named `FirstName`, because users who will be writing ad-hoc queries against the database are accustomed to that name.</span></span> <span data-ttu-id="e39dd-173">若要进行此映射，可使用 `Column` 特性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-173">To make this mapping, you can use the `Column` attribute.</span></span>

<span data-ttu-id="e39dd-174">`Column` 特性指定，创建数据库时，映射到 `FirstMidName` 属性的 `Student` 表的列将被命名为 `FirstName`。</span><span class="sxs-lookup"><span data-stu-id="e39dd-174">The `Column` attribute specifies that when the database is created, the column of the `Student` table that maps to the `FirstMidName` property will be named `FirstName`.</span></span> <span data-ttu-id="e39dd-175">换言之，在代码引用 `Student.FirstMidName` 时，数据来源将是 `Student` 表的 `FirstName` 列或在其中进行更新。</span><span class="sxs-lookup"><span data-stu-id="e39dd-175">In other words, when your code refers to `Student.FirstMidName`, the data will come from or be updated in the `FirstName` column of the `Student` table.</span></span> <span data-ttu-id="e39dd-176">如果未指定列名，则将其指定为与属性名称相同的名称。</span><span class="sxs-lookup"><span data-stu-id="e39dd-176">If you don't specify column names, they are given the same name as the property name.</span></span>

<span data-ttu-id="e39dd-177">将 [system.componentmodel. DataAnnotations](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.aspx) 和 column name 属性的 using 语句添加到 `FirstMidName` 属性，如以下突出显示的代码所示：</span><span class="sxs-lookup"><span data-stu-id="e39dd-177">Add a using statement for [System.ComponentModel.DataAnnotations.Schema](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.aspx) and the column name attribute to the `FirstMidName` property, as shown in the following highlighted code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample5.cs?highlight=4,14)]

<span data-ttu-id="e39dd-178">添加 [列属性](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.columnattribute.aspx) 会更改 SchoolContext 的模型，因此它不会与数据库匹配。</span><span class="sxs-lookup"><span data-stu-id="e39dd-178">The addition of the [Column attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.columnattribute.aspx) changes the model backing the SchoolContext, so it won't match the database.</span></span> <span data-ttu-id="e39dd-179">在 PMC 中输入以下命令以创建另一个迁移：</span><span class="sxs-lookup"><span data-stu-id="e39dd-179">Enter the following commands in the PMC to create another migration:</span></span>

[!code-console[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample6.cmd)]

<span data-ttu-id="e39dd-180">在 **服务器资源管理器** (**数据库资源管理器** 如果使用的是 Express for Web) ，请双击 *Student* 表。</span><span class="sxs-lookup"><span data-stu-id="e39dd-180">In **Server Explorer** (**Database Explorer** if you are using Express for Web), double-click the *Student* table.</span></span>

![](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image4.png)

<span data-ttu-id="e39dd-181">下图显示了在应用前两个迁移之前的原始列名称。</span><span class="sxs-lookup"><span data-stu-id="e39dd-181">The following image shows the original column name as it was before you applied the first two migrations.</span></span> <span data-ttu-id="e39dd-182">除了从更改到的列名以外 `FirstMidName` `FirstName` ，这两个名称列已从长度更改 `MAX` 为50个字符。</span><span class="sxs-lookup"><span data-stu-id="e39dd-182">In addition to the column name changing from `FirstMidName` to `FirstName`, the two name columns have changed from `MAX` length to 50 characters.</span></span>

![](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image5.png)

<span data-ttu-id="e39dd-183">你还可以使用 [熟知的 API](https://msdn.microsoft.com/data/jj591617)进行数据库映射更改，如本教程的后面部分所示。</span><span class="sxs-lookup"><span data-stu-id="e39dd-183">You can also make database mapping changes using the [Fluent API](https://msdn.microsoft.com/data/jj591617), as you'll see later in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="e39dd-184">如果尝试在完成所有这些实体类的创建之前进行编译，可能会出现编译器错误。</span><span class="sxs-lookup"><span data-stu-id="e39dd-184">If you try to compile before you finish creating all of these entity classes, you might get compiler errors.</span></span>

## <a name="create-the-instructor-entity"></a><span data-ttu-id="e39dd-185">创建 Instructor 实体</span><span class="sxs-lookup"><span data-stu-id="e39dd-185">Create the Instructor Entity</span></span>

![Instructor_entity](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image6.png)

<span data-ttu-id="e39dd-187">创建 *Models\Instructor.cs*，将模板代码替换为以下代码：</span><span class="sxs-lookup"><span data-stu-id="e39dd-187">Create *Models\Instructor.cs*, replacing the template code with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample7.cs)]

<span data-ttu-id="e39dd-188">请注意，`Student` 和 `Instructor` 实体中具有几个相同属性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-188">Notice that several properties are the same in the `Student` and `Instructor` entities.</span></span> <span data-ttu-id="e39dd-189">在本系列后面的 [实现继承](implementing-inheritance-with-the-entity-framework-in-an-asp-net-mvc-application.md) 教程中，你将使用继承重构以消除这种冗余。</span><span class="sxs-lookup"><span data-stu-id="e39dd-189">In the [Implementing Inheritance](implementing-inheritance-with-the-entity-framework-in-an-asp-net-mvc-application.md) tutorial later in this series, you'll refactor using inheritance to eliminate this redundancy.</span></span>

### <a name="the-required-and-display-attributes"></a><span data-ttu-id="e39dd-190">必需的和显示的特性</span><span class="sxs-lookup"><span data-stu-id="e39dd-190">The Required and Display Attributes</span></span>

<span data-ttu-id="e39dd-191">属性上的属性 `LastName` 指定它是必填字段，文本框的标题应为 "Last Name" (而不是属性名称，这是 "LastName"，不含空格) ，并且该值长度不能超过50个字符。</span><span class="sxs-lookup"><span data-stu-id="e39dd-191">The attributes on the `LastName` property specify that it's a required field, that the caption for the text box should be "Last Name" (instead of the property name, which would be "LastName" with no space), and that the value can't be longer than 50 characters.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample8.cs)]

<span data-ttu-id="e39dd-192">[StringLength 特性](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx)设置数据库中的最大长度，并为 ASP.NET MVC 提供客户端和服务器端验证。</span><span class="sxs-lookup"><span data-stu-id="e39dd-192">The [StringLength attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) sets the maximum length in the database and provides client side and server side validation for ASP.NET MVC.</span></span> <span data-ttu-id="e39dd-193">还可在此属性中指定最小字符串长度，但最小值对数据库架构没有影响。</span><span class="sxs-lookup"><span data-stu-id="e39dd-193">You can also specify the minimum string length in this attribute, but the minimum value has no impact on the database schema.</span></span> <span data-ttu-id="e39dd-194">值类型（例如 DateTime、int、double 和 float）不需要 [必需的特性](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-194">The [Required attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) is not needed for value types such as DateTime, int, double, and float.</span></span> <span data-ttu-id="e39dd-195">值类型不能赋予 null 值，因此它们原本是必需的。</span><span class="sxs-lookup"><span data-stu-id="e39dd-195">Value types cannot be assigned a null value, so they are inherently required.</span></span> <span data-ttu-id="e39dd-196">您可以删除 [所需的属性](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) ，并将其替换为该属性的最小长度参数 `StringLength` ：</span><span class="sxs-lookup"><span data-stu-id="e39dd-196">You could remove the [Required attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) and replace it with a minimum length parameter for the `StringLength` attribute:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample9.cs?highlight=2)]

<span data-ttu-id="e39dd-197">可以将多个属性放在一行上，因此还可以编写讲师类，如下所示：</span><span class="sxs-lookup"><span data-stu-id="e39dd-197">You can put multiple attributes on one line, so you could also write the instructor class as follows:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample10.cs)]

### <a name="the-fullname-calculated-property"></a><span data-ttu-id="e39dd-198">FullName 计算属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-198">The FullName Calculated Property</span></span>

<span data-ttu-id="e39dd-199">`FullName` 是计算属性，可返回通过串联两个其他属性创建的值。</span><span class="sxs-lookup"><span data-stu-id="e39dd-199">`FullName` is a calculated property that returns a value that's created by concatenating two other properties.</span></span> <span data-ttu-id="e39dd-200">因此，它只有一个 `get` 取值函数，并且 `FullName` 在数据库中不会生成任何列。</span><span class="sxs-lookup"><span data-stu-id="e39dd-200">Therefore it has only a `get` accessor, and no `FullName` column will be generated in the database.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample11.cs)]

### <a name="the-courses-and-officeassignment-navigation-properties"></a><span data-ttu-id="e39dd-201">课程和 OfficeAssignment 导航属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-201">The Courses and OfficeAssignment Navigation Properties</span></span>

<span data-ttu-id="e39dd-202">`Courses` 和 `OfficeAssignment` 是导航属性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-202">The `Courses` and `OfficeAssignment` properties are navigation properties.</span></span> <span data-ttu-id="e39dd-203">如前所述，它们通常定义为 [虚拟](https://msdn.microsoft.com/library/9fkccyh4(v=vs.110).aspx) ，以便能够利用称为 [延迟加载](https://msdn.microsoft.com/magazine/hh205756.aspx)的实体框架功能。</span><span class="sxs-lookup"><span data-stu-id="e39dd-203">As was explained earlier, they are typically defined as [virtual](https://msdn.microsoft.com/library/9fkccyh4(v=vs.110).aspx) so that they can take advantage of an Entity Framework feature called [lazy loading](https://msdn.microsoft.com/magazine/hh205756.aspx).</span></span> <span data-ttu-id="e39dd-204">此外，如果导航属性可以包含多个实体，则其类型必须实现[ICollection &lt; T &gt; ](https://msdn.microsoft.com/library/92t2ye13.aspx)接口。</span><span class="sxs-lookup"><span data-stu-id="e39dd-204">In addition, if a navigation property can hold multiple entities, its type must implement the [ICollection&lt;T&gt;](https://msdn.microsoft.com/library/92t2ye13.aspx) Interface.</span></span> <span data-ttu-id="e39dd-205"> (例如， [IList &lt; t &gt; ](https://msdn.microsoft.com/library/5y536ey6.aspx)限定，而不是[IEnumerable &lt; t &gt; ](https://msdn.microsoft.com/library/9eekhta0.aspx) ，因为 `IEnumerable<T>` 不实现[Add](https://msdn.microsoft.com/library/63ywd54z.aspx)。</span><span class="sxs-lookup"><span data-stu-id="e39dd-205">(For example [IList&lt;T&gt;](https://msdn.microsoft.com/library/5y536ey6.aspx) qualifies but not [IEnumerable&lt;T&gt;](https://msdn.microsoft.com/library/9eekhta0.aspx) because `IEnumerable<T>` doesn't implement [Add](https://msdn.microsoft.com/library/63ywd54z.aspx).</span></span>

<span data-ttu-id="e39dd-206">讲师可以讲授任意数量的课程，因此 `Courses` 将其定义为一个实体集合 `Course` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-206">An instructor can teach any number of courses, so `Courses` is defined as a collection of `Course` entities.</span></span> <span data-ttu-id="e39dd-207">我们的业务规则陈述，一个指导员最多只能有一个办公室，因此，它 `OfficeAssignment` 定义为单个 `OfficeAssignment` 实体 (`null` 如果没有) 的办公室分配，则可能是这种情况。</span><span class="sxs-lookup"><span data-stu-id="e39dd-207">Our business rules state an instructor can only have at most one office, so `OfficeAssignment` is defined as a single `OfficeAssignment` entity (which may be `null` if no office is assigned).</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample12.cs)]

## <a name="create-the-officeassignment-entity"></a><span data-ttu-id="e39dd-208">创建 OfficeAssignment 实体</span><span class="sxs-lookup"><span data-stu-id="e39dd-208">Create the OfficeAssignment Entity</span></span>

![OfficeAssignment_entity](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image7.png)

<span data-ttu-id="e39dd-210">用以下代码创建 *Models\OfficeAssignment.cs* ：</span><span class="sxs-lookup"><span data-stu-id="e39dd-210">Create *Models\OfficeAssignment.cs* with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample13.cs)]

<span data-ttu-id="e39dd-211">生成项目，该项目保存更改并验证编译器能否捕获任何复制和粘贴错误。</span><span class="sxs-lookup"><span data-stu-id="e39dd-211">Build the project, which saves your changes and verifies you haven't made any copy and paste errors the compiler can catch.</span></span>

### <a name="the-key-attribute"></a><span data-ttu-id="e39dd-212">键属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-212">The Key Attribute</span></span>

<span data-ttu-id="e39dd-213">和实体之间存在一对零或一的关系 `Instructor` `OfficeAssignment` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-213">There's a one-to-zero-or-one relationship between the `Instructor` and the `OfficeAssignment` entities.</span></span> <span data-ttu-id="e39dd-214">Office 分配只与分配给它的指导员相关，因此，其主键也是该实体的外键 `Instructor` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-214">An office assignment only exists in relation to the instructor it's assigned to, and therefore its primary key is also its foreign key to the `Instructor` entity.</span></span> <span data-ttu-id="e39dd-215">但实体框架无法自动识别 `InstructorID` 为此实体的主键，因为其名称不遵循 `ID` 或 *classname* `ID` 命名约定。</span><span class="sxs-lookup"><span data-stu-id="e39dd-215">But the Entity Framework can't automatically recognize `InstructorID` as the primary key of this entity because its name doesn't follow the `ID` or *classname* `ID` naming convention.</span></span> <span data-ttu-id="e39dd-216">因此，`Key` 特性用于将其识别为主键：</span><span class="sxs-lookup"><span data-stu-id="e39dd-216">Therefore, the `Key` attribute is used to identify it as the key:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample14.cs)]

<span data-ttu-id="e39dd-217">`Key`如果实体具有其自己的主键，但你想要将属性命名为不同于或的属性，你也可以使用属性 `classnameID` `ID` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-217">You can also use the `Key` attribute if the entity does have its own primary key but you want to name the property something different than `classnameID` or `ID`.</span></span> <span data-ttu-id="e39dd-218">默认情况下，EF 将该键视为非数据库生成，因为列是用于标识关系的。</span><span class="sxs-lookup"><span data-stu-id="e39dd-218">By default EF treats the key as non-database-generated because the column is for an identifying relationship.</span></span>

### <a name="the-foreignkey-attribute"></a><span data-ttu-id="e39dd-219">ForeignKey 特性</span><span class="sxs-lookup"><span data-stu-id="e39dd-219">The ForeignKey Attribute</span></span>

<span data-ttu-id="e39dd-220">如果在两个实体之间存在一对零或一关系或一对一关系 (如介于 `OfficeAssignment` 和 `Instructor`) 之间，则 EF 无法确定关系的哪一端是主体，哪个端依赖于。</span><span class="sxs-lookup"><span data-stu-id="e39dd-220">When there is a one-to-zero-or-one relationship or a one-to-one relationship between two entities (such as between `OfficeAssignment` and `Instructor`), EF can't work out which end of the relationship is the principal and which end is dependent.</span></span> <span data-ttu-id="e39dd-221">一对一关系在每个类中都有一个指向另一个类的引用导航属性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-221">One-to-one relationships have a reference navigation property in each class to the other class.</span></span> <span data-ttu-id="e39dd-222">[ForeignKey 特性](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.foreignkeyattribute.aspx)可应用于依赖类以建立关系。</span><span class="sxs-lookup"><span data-stu-id="e39dd-222">The [ForeignKey Attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.foreignkeyattribute.aspx) can be applied to the dependent class to establish the relationship.</span></span> <span data-ttu-id="e39dd-223">如果省略了 [ForeignKey 属性](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.foreignkeyattribute.aspx)，则在尝试创建迁移时，会收到以下错误：</span><span class="sxs-lookup"><span data-stu-id="e39dd-223">If you omit the [ForeignKey Attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.foreignkeyattribute.aspx), you get the following error when you try to create the migration:</span></span>

<span data-ttu-id="e39dd-224">无法确定类型为 "ContosoUniversity" 和 "ContosoUniversity" 之间的关联的主体端。</span><span class="sxs-lookup"><span data-stu-id="e39dd-224">Unable to determine the principal end of an association between the types 'ContosoUniversity.Models.OfficeAssignment' and 'ContosoUniversity.Models.Instructor'.</span></span> <span data-ttu-id="e39dd-225">必须使用关系 Fluent API 或数据批注显式配置此关联的主体端。</span><span class="sxs-lookup"><span data-stu-id="e39dd-225">The principal end of this association must be explicitly configured using either the relationship fluent API or data annotations.</span></span>

<span data-ttu-id="e39dd-226">稍后在本教程中，我们将演示如何配置此关系与 Fluent API。</span><span class="sxs-lookup"><span data-stu-id="e39dd-226">Later in the tutorial we'll show how to configure this relationship with the fluent API.</span></span>

### <a name="the-instructor-navigation-property"></a><span data-ttu-id="e39dd-227">讲师导航属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-227">The Instructor Navigation Property</span></span>

<span data-ttu-id="e39dd-228">`Instructor`实体具有可为 null 的 `OfficeAssignment` 导航属性 (，因为讲师可能不具有 office 分配) ，而 `OfficeAssignment` 实体具有不可为 null 的 `Instructor` 导航属性 (因为不存在讲师的办公室分配不能为 `InstructorID`) null。</span><span class="sxs-lookup"><span data-stu-id="e39dd-228">The `Instructor` entity has a nullable `OfficeAssignment` navigation property (because an instructor might not have an office assignment), and the `OfficeAssignment` entity has a non-nullable `Instructor` navigation property (because an office assignment can't exist without an instructor -- `InstructorID` is non-nullable).</span></span> <span data-ttu-id="e39dd-229">当 `Instructor` 实体具有相关实体时 `OfficeAssignment` ，每个实体都将在其导航属性中引用另一个实体。</span><span class="sxs-lookup"><span data-stu-id="e39dd-229">When an `Instructor` entity has a related `OfficeAssignment` entity, each entity will have a reference to the other one in its navigation property.</span></span>

<span data-ttu-id="e39dd-230">您可以 `[Required]` 在讲师导航属性中放置一个属性，以指定必须有相关讲师，但您不必这样做，因为 InstructorID 外键 (也是此表的键) 不可为 null。</span><span class="sxs-lookup"><span data-stu-id="e39dd-230">You could put a `[Required]` attribute on the Instructor navigation property to specify that there must be a related instructor, but you don't have to do that because the InstructorID foreign key (which is also the key to this table) is non-nullable.</span></span>

## <a name="modify-the-course-entity"></a><span data-ttu-id="e39dd-231">修改 Course 实体</span><span class="sxs-lookup"><span data-stu-id="e39dd-231">Modify the Course Entity</span></span>

![Course_entity](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image8.png)

<span data-ttu-id="e39dd-233">在 *Models\Course.cs* 中，将之前添加的代码替换为以下代码：</span><span class="sxs-lookup"><span data-stu-id="e39dd-233">In *Models\Course.cs*, replace the code you added earlier with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample15.cs)]

<span data-ttu-id="e39dd-234">课程实体具有外键属性， `DepartmentID` 该属性指向相关 `Department` 实体并且具有 `Department` 导航属性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-234">The course entity has a foreign key property `DepartmentID` which points to the related `Department` entity and it has a `Department` navigation property.</span></span> <span data-ttu-id="e39dd-235">如果拥有相关实体的导航属性，则 Entity Framework 不会要求为数据模型添加外键属性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-235">The Entity Framework doesn't require you to add a foreign key property to your data model when you have a navigation property for a related entity.</span></span> <span data-ttu-id="e39dd-236">EF 在需要时在数据库中自动创建外键。</span><span class="sxs-lookup"><span data-stu-id="e39dd-236">EF automatically creates foreign keys in the database wherever they are needed.</span></span> <span data-ttu-id="e39dd-237">但如果数据模型包含外键，则更新会变得更简单、更高效。</span><span class="sxs-lookup"><span data-stu-id="e39dd-237">But having the foreign key in the data model can make updates simpler and more efficient.</span></span> <span data-ttu-id="e39dd-238">例如，当您提取要编辑的课程实体时， `Department` 如果不加载它，则实体为 null，因此，在更新课程实体时，必须首先提取该 `Department` 实体。</span><span class="sxs-lookup"><span data-stu-id="e39dd-238">For example, when you fetch a course entity to edit, the `Department` entity is null if you don't load it, so when you update the course entity, you would have to first fetch the `Department` entity.</span></span> <span data-ttu-id="e39dd-239">如果 `DepartmentID` 数据模型中包含外键属性，则不需要在 `Department` 更新前提取实体。</span><span class="sxs-lookup"><span data-stu-id="e39dd-239">When the foreign key property `DepartmentID` is included in the data model, you don't need to fetch the `Department` entity before you update.</span></span>

### <a name="the-databasegenerated-attribute"></a><span data-ttu-id="e39dd-240">DatabaseGenerated 特性</span><span class="sxs-lookup"><span data-stu-id="e39dd-240">The DatabaseGenerated Attribute</span></span>

<span data-ttu-id="e39dd-241">属性上带有[None](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.databasegeneratedoption(v=vs.110).aspx)参数的[DatabaseGenerated 特性](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.databasegeneratedattribute.aspx) `CourseID` 指定，主键值由用户提供，而不是由数据库生成。</span><span class="sxs-lookup"><span data-stu-id="e39dd-241">The [DatabaseGenerated attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.databasegeneratedattribute.aspx) with the [None](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.databasegeneratedoption(v=vs.110).aspx) parameter on the `CourseID` property specifies that primary key values are provided by the user rather than generated by the database.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample16.cs)]

<span data-ttu-id="e39dd-242">默认情况下，实体框架假定数据库生成主键值。</span><span class="sxs-lookup"><span data-stu-id="e39dd-242">By default, the Entity Framework assumes that primary key values are generated by the database.</span></span> <span data-ttu-id="e39dd-243">大多数情况下，这是理想情况。</span><span class="sxs-lookup"><span data-stu-id="e39dd-243">That's what you want in most scenarios.</span></span> <span data-ttu-id="e39dd-244">但是，对于 `Course` 实体，你将使用用户指定的课程编号，如一个部门的1000系列、另一部门的2000系列等等。</span><span class="sxs-lookup"><span data-stu-id="e39dd-244">However, for `Course` entities, you'll use a user-specified course number such as a 1000 series for one department, a 2000 series for another department, and so on.</span></span>

### <a name="foreign-key-and-navigation-properties"></a><span data-ttu-id="e39dd-245">外键和导航属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-245">Foreign Key and Navigation Properties</span></span>

<span data-ttu-id="e39dd-246">实体中的外键属性和导航属性 `Course` 反映以下关系：</span><span class="sxs-lookup"><span data-stu-id="e39dd-246">The foreign key properties and navigation properties in the `Course` entity reflect the following relationships:</span></span>

- <span data-ttu-id="e39dd-247">向一个系分配课程后，出于上述原因，会出现 `DepartmentID` 外键和 `Department` 导航属性。</span><span class="sxs-lookup"><span data-stu-id="e39dd-247">A course is assigned to one department, so there's a `DepartmentID` foreign key and a `Department` navigation property for the reasons mentioned above.</span></span> 

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample17.cs)]
- <span data-ttu-id="e39dd-248">参与一门课程的学生数量不定，因此 `Enrollments` 导航属性是一个集合：</span><span class="sxs-lookup"><span data-stu-id="e39dd-248">A course can have any number of students enrolled in it, so the `Enrollments` navigation property is a collection:</span></span> 

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample18.cs)]
- <span data-ttu-id="e39dd-249">一门课程可能由多位讲师讲授，因此 `Instructors` 导航属性是一个集合：</span><span class="sxs-lookup"><span data-stu-id="e39dd-249">A course may be taught by multiple instructors, so the `Instructors` navigation property is a collection:</span></span> 

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample19.cs)]

## <a name="creating-the-department-entity"></a><span data-ttu-id="e39dd-250">创建部门实体</span><span class="sxs-lookup"><span data-stu-id="e39dd-250">Creating the Department Entity</span></span>

![Department_entity](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image9.png)

<span data-ttu-id="e39dd-252">用以下代码创建 *Models\Department.cs* ：</span><span class="sxs-lookup"><span data-stu-id="e39dd-252">Create *Models\Department.cs* with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample20.cs)]

### <a name="the-column-attribute"></a><span data-ttu-id="e39dd-253">列属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-253">The Column Attribute</span></span>

<span data-ttu-id="e39dd-254">之前，你已使用 [列属性](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.columnattribute.aspx) 更改列名映射。</span><span class="sxs-lookup"><span data-stu-id="e39dd-254">Earlier you used the [Column attribute](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.schema.columnattribute.aspx) to change column name mapping.</span></span> <span data-ttu-id="e39dd-255">在实体的代码中 `Department` ， `Column` 特性用于更改 SQL 数据类型映射，以便使用数据库中的 SQL Server [money](https://msdn.microsoft.com/library/ms179882.aspx) 类型定义列：</span><span class="sxs-lookup"><span data-stu-id="e39dd-255">In the code for the `Department` entity, the `Column` attribute is being used to change SQL data type mapping so that the column will be defined using the SQL Server [money](https://msdn.microsoft.com/library/ms179882.aspx) type in the database:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample21.cs)]

<span data-ttu-id="e39dd-256">通常不需要列映射，因为实体框架通常基于您为属性定义的 CLR 类型选择适当的 SQL Server 数据类型。</span><span class="sxs-lookup"><span data-stu-id="e39dd-256">Column mapping is generally not required, because the Entity Framework usually chooses the appropriate SQL Server data type based on the CLR type that you define for the property.</span></span> <span data-ttu-id="e39dd-257">CLR `decimal` 类型会映射到 SQL Server `decimal` 类型。</span><span class="sxs-lookup"><span data-stu-id="e39dd-257">The CLR `decimal` type maps to a SQL Server `decimal` type.</span></span> <span data-ttu-id="e39dd-258">但在这种情况下，您知道列将包含货币金额，而 [money](https://msdn.microsoft.com/library/ms179882.aspx) 数据类型则更适合这样做。</span><span class="sxs-lookup"><span data-stu-id="e39dd-258">But in this case you know that the column will be holding currency amounts, and the [money](https://msdn.microsoft.com/library/ms179882.aspx) data type is more appropriate for that.</span></span>

### <a name="foreign-key-and-navigation-properties"></a><span data-ttu-id="e39dd-259">外键和导航属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-259">Foreign Key and Navigation Properties</span></span>

<span data-ttu-id="e39dd-260">外键和导航属性可反映以下关系：</span><span class="sxs-lookup"><span data-stu-id="e39dd-260">The foreign key and navigation properties reflect the following relationships:</span></span>

- <span data-ttu-id="e39dd-261">一个系可能有也可能没有管理员，而管理员始终是讲师。</span><span class="sxs-lookup"><span data-stu-id="e39dd-261">A department may or may not have an administrator, and an administrator is always an instructor.</span></span> <span data-ttu-id="e39dd-262">因此，该 `InstructorID` 属性包含为实体的外键 `Instructor` ，而在类型标识之后添加一个问号， `int` 以将该属性标记为可为 null。导航属性名为， `Administrator` 但包含 `Instructor` 实体：</span><span class="sxs-lookup"><span data-stu-id="e39dd-262">Therefore the `InstructorID` property is included as the foreign key to the `Instructor` entity, and a question mark is added after the `int` type designation to mark the property as nullable.The navigation property is named `Administrator` but holds an `Instructor` entity:</span></span> 

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample22.cs)]
- <span data-ttu-id="e39dd-263">一个部门可能有许多课程，因此存在一个 `Courses` 导航属性：</span><span class="sxs-lookup"><span data-stu-id="e39dd-263">A department may have many courses, so there's a `Courses` navigation property:</span></span> 

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample23.cs)]

  > [!NOTE]
  > <span data-ttu-id="e39dd-264">按照约定，Entity Framework 能针对不可为 null 的外键和多对多关系启用级联删除。</span><span class="sxs-lookup"><span data-stu-id="e39dd-264">By convention, the Entity Framework enables cascade delete for non-nullable foreign keys and for many-to-many relationships.</span></span> <span data-ttu-id="e39dd-265">这可能会导致循环级联删除规则，这会导致在初始值设定项代码运行时出现异常。</span><span class="sxs-lookup"><span data-stu-id="e39dd-265">This can result in circular cascade delete rules, which will cause an exception when your initializer code runs.</span></span> <span data-ttu-id="e39dd-266">例如，如果未将 `Department.InstructorID` 属性定义为可为 null，则在初始值设定项运行时，会收到以下异常消息： "引用关系将导致不允许的循环引用。"</span><span class="sxs-lookup"><span data-stu-id="e39dd-266">For example, if you didn't define the `Department.InstructorID` property as nullable, you'd get the following exception message when the initializer runs: "The referential relationship will result in a cyclical reference that's not allowed."</span></span> <span data-ttu-id="e39dd-267">如果业务规则 "必需" `InstructorID` 属性为不可为 null，则必须使用以下 Fluent API 禁用关系的级联删除：</span><span class="sxs-lookup"><span data-stu-id="e39dd-267">If your business rules required `InstructorID` property as non-nullable, you would have to use the following fluent API to disable cascade delete on the relationship:</span></span> 

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample24.cs)]

## <a name="modifying-the-student-entity"></a><span data-ttu-id="e39dd-268">修改 Student 实体</span><span class="sxs-lookup"><span data-stu-id="e39dd-268">Modifying the Student Entity</span></span>

![Student_entity](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image10.png)

<span data-ttu-id="e39dd-270">在 *Models\Student.cs* 中，将之前添加的代码替换为以下代码。</span><span class="sxs-lookup"><span data-stu-id="e39dd-270">In *Models\Student.cs*, replace the code you added earlier with the following code.</span></span> <span data-ttu-id="e39dd-271">突出显示所作更改。</span><span class="sxs-lookup"><span data-stu-id="e39dd-271">The changes are highlighted.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample25.cs?highlight=12,15,24-27)]

## <a name="the-enrollment-entity"></a><span data-ttu-id="e39dd-272">注册实体</span><span class="sxs-lookup"><span data-stu-id="e39dd-272">The Enrollment Entity</span></span>

 <span data-ttu-id="e39dd-273">在 *Models\Enrollment.cs* 中，将之前添加的代码替换为以下代码</span><span class="sxs-lookup"><span data-stu-id="e39dd-273">In *Models\Enrollment.cs*, replace the code you added earlier with the following code</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample26.cs?highlight=16)]

### <a name="foreign-key-and-navigation-properties"></a><span data-ttu-id="e39dd-274">外键和导航属性</span><span class="sxs-lookup"><span data-stu-id="e39dd-274">Foreign Key and Navigation Properties</span></span>

<span data-ttu-id="e39dd-275">外键属性和导航属性可反映以下关系：</span><span class="sxs-lookup"><span data-stu-id="e39dd-275">The foreign key properties and navigation properties reflect the following relationships:</span></span>

- <span data-ttu-id="e39dd-276">注册记录面向一门课程，因此存在 `CourseID` 外键属性和 `Course` 导航属性：</span><span class="sxs-lookup"><span data-stu-id="e39dd-276">An enrollment record is for a single course, so there's a `CourseID` foreign key property and a `Course` navigation property:</span></span> 

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample27.cs)]
- <span data-ttu-id="e39dd-277">注册记录面向一名学生，因此存在 `StudentID` 外键属性和 `Student` 导航属性：</span><span class="sxs-lookup"><span data-stu-id="e39dd-277">An enrollment record is for a single student, so there's a `StudentID` foreign key property and a `Student` navigation property:</span></span> 

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample28.cs)]

### <a name="many-to-many-relationships"></a><span data-ttu-id="e39dd-278">多对多关系</span><span class="sxs-lookup"><span data-stu-id="e39dd-278">Many-to-Many Relationships</span></span>

<span data-ttu-id="e39dd-279">和实体之间存在多对多关系 `Student` `Course` ，并且 `Enrollment` 实体将作为具有数据库中的 *有效负载* 的多对多联接表。</span><span class="sxs-lookup"><span data-stu-id="e39dd-279">There's a many-to-many relationship between the `Student` and `Course` entities, and the `Enrollment` entity functions as a many-to-many join table *with payload* in the database.</span></span> <span data-ttu-id="e39dd-280">这意味着 `Enrollment` ，对于联接的表，该表除了包含外键之外还包含其他数据 (在本例中，为主键和 `Grade` 属性) 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-280">This means that the `Enrollment` table contains additional data besides foreign keys for the joined tables (in this case, a primary key and a `Grade` property).</span></span>

<span data-ttu-id="e39dd-281">下图显示这些关系在实体关系图中的外观。</span><span class="sxs-lookup"><span data-stu-id="e39dd-281">The following illustration shows what these relationships look like in an entity diagram.</span></span> <span data-ttu-id="e39dd-282"> (此关系图是使用 [实体框架的 Power Tools](https://visualstudiogallery.msdn.microsoft.com/72a60b14-1581-4b9b-89f2-846072eff19d)生成的;创建该关系图不是本教程的一部分，这里只是将其用作图例。 ) </span><span class="sxs-lookup"><span data-stu-id="e39dd-282">(This diagram was generated using the [Entity Framework Power Tools](https://visualstudiogallery.msdn.microsoft.com/72a60b14-1581-4b9b-89f2-846072eff19d); creating the diagram isn't part of the tutorial, it's just being used here as an illustration.)</span></span>

![学生-Course_many-many_relationship](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image11.png)

<span data-ttu-id="e39dd-284">每个关系线在一端有一个1，星号 (\* 彼此) ，指示一对多关系。</span><span class="sxs-lookup"><span data-stu-id="e39dd-284">Each relationship line has a 1 at one end and an asterisk (\*) at the other, indicating a one-to-many relationship.</span></span>

<span data-ttu-id="e39dd-285">如果 `Enrollment` 表未包括评分信息，只需包含两个外键 `CourseID` 和 `StudentID` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-285">If the `Enrollment` table didn't include grade information, it would only need to contain the two foreign keys `CourseID` and `StudentID`.</span></span> <span data-ttu-id="e39dd-286">在这种情况下，它会与多对多联接表相对应， *而无需负载* (或 *纯联接表*) 在数据库中，而无需为其创建模型类。</span><span class="sxs-lookup"><span data-stu-id="e39dd-286">In that case, it would correspond to a many-to-many join table *without payload* (or a *pure join table*) in the database, and you wouldn't have to create a model class for it at all.</span></span> <span data-ttu-id="e39dd-287">`Instructor`和 `Course` 实体具有这种类型的多对多关系，如您所见，它们之间没有实体类：</span><span class="sxs-lookup"><span data-stu-id="e39dd-287">The `Instructor` and `Course` entities have that kind of many-to-many relationship, and as you can see, there is no entity class between them:</span></span>

![讲师 Course_many many_relationship](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image12.png)

<span data-ttu-id="e39dd-289">但是，数据库中需要联接表，如以下数据库关系图中所示：</span><span class="sxs-lookup"><span data-stu-id="e39dd-289">A join table is required in the database, however, as shown in the following database diagram:</span></span>

![讲师 Course_many many_relationship_tables](https://asp.net/media/2577802/Windows-Live-Writer_Creating-a.NET-MVC-Application-4-of-10h1_B662_Instructor-Course_many-to-many_relationship_tables_03e042cf-db89-4b4c-985a-e458351ada76.png)

<span data-ttu-id="e39dd-291">实体框架会自动创建 `CourseInstructor` 表，并通过读取和更新 `Instructor.Courses` 和导航属性来间接读取和更新表 `Course.Instructors` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-291">The Entity Framework automatically creates the `CourseInstructor` table, and you read and update it indirectly by reading and updating the `Instructor.Courses` and `Course.Instructors` navigation properties.</span></span>

## <a name="entity-diagram-showing-relationships"></a><span data-ttu-id="e39dd-292">显示关系的实体关系图</span><span class="sxs-lookup"><span data-stu-id="e39dd-292">Entity Diagram Showing Relationships</span></span>

<span data-ttu-id="e39dd-293">下图显示 Entity Framework Power Tools 针对已完成的学校模型创建的关系图。</span><span class="sxs-lookup"><span data-stu-id="e39dd-293">The following illustration shows the diagram that the Entity Framework Power Tools create for the completed School model.</span></span>

![School_data_model_diagram](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image13.png)

<span data-ttu-id="e39dd-295">除了多对多的关系线外 (\* \*) 和一对多关系线 (1 \*) 。您可以在此处看到从1到 (1 到 0) 之间的一对零或一对多的关系线。在 `Instructor` 和 `OfficeAssignment` 实体之间， (0. 1 0. 1 之间的关系线，以便 \* 在讲师和部门实体之间) 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-295">Besides the many-to-many relationship lines (\* to \*) and the one-to-many relationship lines (1 to \*), you can see here the one-to-zero-or-one relationship line (1 to 0..1) between the `Instructor` and `OfficeAssignment` entities and the zero-or-one-to-many relationship line (0..1 to \*) between the Instructor and Department entities.</span></span>

## <a name="customize-the-data-model-by-adding-code-to-the-database-context"></a><span data-ttu-id="e39dd-296">通过将代码添加到数据库上下文来自定义数据模型</span><span class="sxs-lookup"><span data-stu-id="e39dd-296">Customize the Data Model by adding Code to the Database Context</span></span>

<span data-ttu-id="e39dd-297">接下来，将新实体添加到 `SchoolContext` 类，并使用 [Fluent API](https://msdn.microsoft.com/data/jj591617) 调用自定义某些映射。</span><span class="sxs-lookup"><span data-stu-id="e39dd-297">Next you'll add the new entities to the `SchoolContext` class and customize some of the mapping using [fluent API](https://msdn.microsoft.com/data/jj591617) calls.</span></span> <span data-ttu-id="e39dd-298"> (API 是 "熟知的"，因为它通常通过将一系列方法调用一起排列为单个语句来使用。 ) </span><span class="sxs-lookup"><span data-stu-id="e39dd-298">(The API is "fluent" because it's often used by stringing a series of method calls together into a single statement.)</span></span>

<span data-ttu-id="e39dd-299">在本教程中，你将仅对不能使用属性执行的数据库映射使用 Fluent API。</span><span class="sxs-lookup"><span data-stu-id="e39dd-299">In this tutorial you'll use the fluent API only for database mapping that you can't do with attributes.</span></span> <span data-ttu-id="e39dd-300">但 Fluent API 还可用于指定大多数格式化、验证和映射规则，这可通过特性完成。</span><span class="sxs-lookup"><span data-stu-id="e39dd-300">However, you can also use the fluent API to specify most of the formatting, validation, and mapping rules that you can do by using attributes.</span></span> <span data-ttu-id="e39dd-301">`MinimumLength` 等特性不能通过 Fluent API 应用。</span><span class="sxs-lookup"><span data-stu-id="e39dd-301">Some attributes such as `MinimumLength` can't be applied with the fluent API.</span></span> <span data-ttu-id="e39dd-302">如前所述， `MinimumLength` 不会更改架构，它只应用客户端和服务器端验证规则。</span><span class="sxs-lookup"><span data-stu-id="e39dd-302">As mentioned previously, `MinimumLength` doesn't change the schema, it only applies a client and server side validation rule</span></span>

<span data-ttu-id="e39dd-303">某些开发者倾向于仅使用 Fluent API 以保持实体类的“纯净”。</span><span class="sxs-lookup"><span data-stu-id="e39dd-303">Some developers prefer to use the fluent API exclusively so that they can keep their entity classes "clean."</span></span> <span data-ttu-id="e39dd-304">如有需要，可混合使用特性和 Fluent API，且有些自定义只能通过 Fluent API 实现，但通常建议选择一种方法并尽可能坚持使用这一种。</span><span class="sxs-lookup"><span data-stu-id="e39dd-304">You can mix attributes and fluent API if you want, and there are a few customizations that can only be done by using fluent API, but in general the recommended practice is to choose one of these two approaches and use that consistently as much as possible.</span></span>

<span data-ttu-id="e39dd-305">若要将新实体添加到数据模型，并使用属性执行你不执行的数据库映射，请将 *DAL\SchoolContext.cs* 中的代码替换为以下代码：</span><span class="sxs-lookup"><span data-stu-id="e39dd-305">To add the new entities to the data model and perform database mapping that you didn't do by using attributes, replace the code in *DAL\SchoolContext.cs* with the following code:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample29.cs)]

<span data-ttu-id="e39dd-306">[OnModelCreating](https://msdn.microsoft.com/library/system.data.entity.dbcontext.onmodelcreating(v=vs.103).aspx)方法中的新语句配置多对多联接表：</span><span class="sxs-lookup"><span data-stu-id="e39dd-306">The new statement in the [OnModelCreating](https://msdn.microsoft.com/library/system.data.entity.dbcontext.onmodelcreating(v=vs.103).aspx) method configures the many-to-many join table:</span></span>

- <span data-ttu-id="e39dd-307">对于和实体之间的多对多关系 `Instructor` `Course` ，代码指定联接表的表名和列名。</span><span class="sxs-lookup"><span data-stu-id="e39dd-307">For the many-to-many relationship between the `Instructor` and `Course` entities, the code specifies the table and column names for the join table.</span></span> <span data-ttu-id="e39dd-308">Code First 可以为你配置多对多关系，而无需此代码，但如果不调用此代码，则将为列获取默认名称，例如 `InstructorInstructorID` `InstructorID` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-308">Code First can configure the many-to-many relationship for you without this code, but if you don't call it, you will get default names such as `InstructorInstructorID` for the `InstructorID` column.</span></span>

    [!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample30.cs)]

<span data-ttu-id="e39dd-309">下面的代码提供了一个示例，说明如何使用 Fluent API 而不是特性来指定和实体之间的 `Instructor` 关系 `OfficeAssignment` ：</span><span class="sxs-lookup"><span data-stu-id="e39dd-309">The following code provides an example of how you could have used fluent API instead of attributes to specify the relationship between the `Instructor` and `OfficeAssignment` entities:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample31.cs)]

<span data-ttu-id="e39dd-310">有关 "Fluent API" 语句在幕后执行的操作的信息，请参阅 " [流畅 API](https://blogs.msdn.com/b/aspnetue/archive/2011/05/04/entity-framework-code-first-tutorial-supplement-what-is-going-on-in-a-fluent-api-call.aspx) " 博客文章。</span><span class="sxs-lookup"><span data-stu-id="e39dd-310">For information about what "fluent API" statements are doing behind the scenes, see the [Fluent API](https://blogs.msdn.com/b/aspnetue/archive/2011/05/04/entity-framework-code-first-tutorial-supplement-what-is-going-on-in-a-fluent-api-call.aspx) blog post.</span></span>

## <a name="seed-the-database-with-test-data"></a><span data-ttu-id="e39dd-311">使用测试数据设定数据库种子</span><span class="sxs-lookup"><span data-stu-id="e39dd-311">Seed the Database with Test Data</span></span>

<span data-ttu-id="e39dd-312">将 *Migrations\Configuration.cs* 文件中的代码替换为以下代码，以便为你创建的新实体提供种子数据。</span><span class="sxs-lookup"><span data-stu-id="e39dd-312">Replace the code in the *Migrations\Configuration.cs* file with the following code in order to provide seed data for the new entities you've created.</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample32.cs)]

<span data-ttu-id="e39dd-313">正如你在第一个教程中看到的那样，此代码中的大多数只是更新或创建新的实体对象，并根据测试需要将示例数据加载到属性中。</span><span class="sxs-lookup"><span data-stu-id="e39dd-313">As you saw in the first tutorial, most of this code simply updates or creates new entity objects and loads sample data into properties as required for testing.</span></span> <span data-ttu-id="e39dd-314">但请注意，如何 `Course` 处理与实体具有多对多关系的实体 `Instructor` ：</span><span class="sxs-lookup"><span data-stu-id="e39dd-314">However, notice how the `Course` entity, which has a many-to-many relationship with the `Instructor` entity, is handled:</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample33.cs)]

<span data-ttu-id="e39dd-315">创建 `Course` 对象时，可以 `Instructors` 使用代码将导航属性初始化为空集合 `Instructors = new List<Instructor>()` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-315">When you create a `Course` object, you initialize the `Instructors` navigation property as an empty collection using the code `Instructors = new List<Instructor>()`.</span></span> <span data-ttu-id="e39dd-316">这样，便可以 `Instructor` 使用方法添加与此相关的实体 `Course` `Instructors.Add` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-316">This makes it possible to add `Instructor` entities that are related to this `Course` by using the `Instructors.Add` method.</span></span> <span data-ttu-id="e39dd-317">如果未创建空列表，则无法添加这些关系，因为 `Instructors` 属性将为 null，并且不具有 `Add` 方法。</span><span class="sxs-lookup"><span data-stu-id="e39dd-317">If you didn't create an empty list, you wouldn't be able to add these relationships, because the `Instructors` property would be null and wouldn't have an `Add` method.</span></span> <span data-ttu-id="e39dd-318">你还可以将列表初始化添加到构造函数。</span><span class="sxs-lookup"><span data-stu-id="e39dd-318">You could also add the list initialization to the constructor.</span></span>

## <a name="add-a-migration-and-update-the-database"></a><span data-ttu-id="e39dd-319">添加迁移并更新数据库</span><span class="sxs-lookup"><span data-stu-id="e39dd-319">Add a Migration and Update the Database</span></span>

<span data-ttu-id="e39dd-320">在 PMC 中，输入 `add-migration` 以下命令：</span><span class="sxs-lookup"><span data-stu-id="e39dd-320">From the PMC, enter the `add-migration` command:</span></span>

`PM> add-Migration Chap4`

<span data-ttu-id="e39dd-321">如果此时尝试更新数据库，将收到以下错误：</span><span class="sxs-lookup"><span data-stu-id="e39dd-321">If you try to update the database at this point, you'll get the following error:</span></span>

<span data-ttu-id="e39dd-322">*ALTER TABLE 语句与外键约束 "FK dbo" 冲突 \_ 。课程 \_ dbo。部门 \_ DepartmentID "。在表 "dbo" 的数据库 "ContosoUniversity" 中发生冲突。部门 "，列" DepartmentID "。*</span><span class="sxs-lookup"><span data-stu-id="e39dd-322">*The ALTER TABLE statement conflicted with the FOREIGN KEY constraint "FK\_dbo.Course\_dbo.Department\_DepartmentID". The conflict occurred in database "ContosoUniversity", table "dbo.Department", column 'DepartmentID'.*</span></span>

<span data-ttu-id="e39dd-323">编辑 &lt; *时间戳 &gt; \_ Chap4.cs* 文件，并进行以下代码更改 (你将添加 SQL 语句并修改 `AddColumn` 语句) ：</span><span class="sxs-lookup"><span data-stu-id="e39dd-323">Edit the &lt;*timestamp&gt;\_Chap4.cs* file, and make the following code changes (you'll add a SQL statement and modify an `AddColumn` statement):</span></span>

[!code-csharp[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample34.cs?highlight=14-18)]

<span data-ttu-id="e39dd-324"> (确保在添加新行时注释掉或删除现有 `AddColumn` 行，否则在输入命令时将收到错误 `update-database` 。 ) </span><span class="sxs-lookup"><span data-stu-id="e39dd-324">(Make sure that you comment out or delete the existing `AddColumn` line when you add the new one, or you'll get an error when you enter the `update-database` command.)</span></span>

<span data-ttu-id="e39dd-325">有时，当您执行与现有数据的迁移时，您需要将存根（stub）数据插入到数据库中以满足外键约束，这就是您现在要做的。</span><span class="sxs-lookup"><span data-stu-id="e39dd-325">Sometimes when you execute migrations with existing data, you need to insert stub data into the database to satisfy foreign key constraints, and that's what you're doing now.</span></span> <span data-ttu-id="e39dd-326">生成的代码将不可为 null 的 `DepartmentID` 外键添加到 `Course` 表中。</span><span class="sxs-lookup"><span data-stu-id="e39dd-326">The generated code adds a non-nullable `DepartmentID` foreign key to the `Course` table.</span></span> <span data-ttu-id="e39dd-327">如果在 `Course` 代码运行时表中已有行，则 `AddColumn` 操作将失败，因为 SQL Server 不知道要放入列中的值不能为 null。</span><span class="sxs-lookup"><span data-stu-id="e39dd-327">If there are already rows in the `Course` table when the code runs, the `AddColumn` operation would fail because SQL Server doesn't know what value to put in the column that can't be null.</span></span> <span data-ttu-id="e39dd-328">因此，您已经更改了代码，为新列指定了默认值，并且已创建了一个名为 "Temp" 的存根部作为默认部门。</span><span class="sxs-lookup"><span data-stu-id="e39dd-328">Therefore you've changed the code to give the new column a default value, and you've created a stub department named "Temp" to act as the default department.</span></span> <span data-ttu-id="e39dd-329">因此，如果 `Course` 此代码运行时存在现有行，它们将与 "Temp" 部门相关。</span><span class="sxs-lookup"><span data-stu-id="e39dd-329">As a result, if there are existing `Course` rows when this code runs, they will all be related to the "Temp" department.</span></span>

<span data-ttu-id="e39dd-330">当该 `Seed` 方法运行时，它将在表中插入行 `Department` ，并将现有 `Course` 行与这些新 `Department` 行相关。</span><span class="sxs-lookup"><span data-stu-id="e39dd-330">When the `Seed` method runs, it will insert rows in the `Department` table and it will relate existing `Course` rows to those new `Department` rows.</span></span> <span data-ttu-id="e39dd-331">如果你尚未在 UI 中添加任何课程，则不再需要 "Temp" 部门或列的默认值 `Course.DepartmentID` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-331">If you haven't added any courses in the UI, you would then no longer need the "Temp" department or the default value on the `Course.DepartmentID` column.</span></span> <span data-ttu-id="e39dd-332">若要允许有人使用应用程序添加了课程，你还需要更新 `Seed` 方法代码，以确保在 `Course` `Seed` `DepartmentID` 删除列中的默认值并删除 "Temp" 部之前，所有行 (的方法不只是之前运行的方法) 具有有效值。</span><span class="sxs-lookup"><span data-stu-id="e39dd-332">To allow for the possibility that someone might have added courses by using the application, you'd also want to update the `Seed` method code to ensure that all `Course` rows (not just the ones inserted by earlier runs of the `Seed` method) have valid `DepartmentID` values before you remove the default value from the column and delete the "Temp" department.</span></span>

<span data-ttu-id="e39dd-333">完成 &lt; *&gt; \_ Chap4.cs* 文件的编辑后，请 `update-database` 在 PMC 中输入命令来执行迁移。</span><span class="sxs-lookup"><span data-stu-id="e39dd-333">After you have finished editing the &lt;*timestamp&gt;\_Chap4.cs* file, enter the `update-database` command in the PMC to execute the migration.</span></span>

> [!NOTE]
> <span data-ttu-id="e39dd-334">迁移数据和进行架构更改时，可能会收到其他错误。</span><span class="sxs-lookup"><span data-stu-id="e39dd-334">It's possible to get other errors when migrating data and making schema changes.</span></span> <span data-ttu-id="e39dd-335">如果收到无法解决的迁移错误，可以更改 *Web.config* 文件中的连接字符串，也可以删除数据库。</span><span class="sxs-lookup"><span data-stu-id="e39dd-335">If you get migration errors you can't resolve, you can either change the connection string in the *Web.config* file or delete the database.</span></span> <span data-ttu-id="e39dd-336">最简单的方法是重命名 *Web.config* 文件中的数据库。</span><span class="sxs-lookup"><span data-stu-id="e39dd-336">The simplest approach is to rename the database in *Web.config* file.</span></span> <span data-ttu-id="e39dd-337">例如，将数据库名称更改为 CU 测试，如下 \_ 所示：</span><span class="sxs-lookup"><span data-stu-id="e39dd-337">For example, change the database name to CU\_test as shown in the following:</span></span>
> 
> [!code-xml[Main](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/samples/sample35.xml?highlight=1-2)]
> 
> <span data-ttu-id="e39dd-338">对于新数据库，没有要迁移的数据，并且 `update-database` 命令更有可能在没有错误的情况下完成。</span><span class="sxs-lookup"><span data-stu-id="e39dd-338">With a new database, there is no data to migrate, and the `update-database` command is much more likely to complete without errors.</span></span> <span data-ttu-id="e39dd-339">有关如何删除数据库的说明，请参阅 [如何从 Visual Studio 2012 中删除数据库](http://romiller.com/2013/05/17/how-to-drop-a-database-from-visual-studio-2012/)。</span><span class="sxs-lookup"><span data-stu-id="e39dd-339">For instructions on how to delete the database, see [How to Drop a Database from Visual Studio 2012](http://romiller.com/2013/05/17/how-to-drop-a-database-from-visual-studio-2012/).</span></span>

<span data-ttu-id="e39dd-340">像之前一样，在 **服务器资源管理器** 中打开数据库，然后展开 " **表** " 节点以查看是否已创建所有表。</span><span class="sxs-lookup"><span data-stu-id="e39dd-340">Open the database in **Server Explorer** as you did earlier, and expand the **Tables** node to see that all of the tables have been created.</span></span> <span data-ttu-id="e39dd-341"> (如果仍 **服务器资源管理器** 从以前的时间打开，请单击 " **刷新** " 按钮。 ) </span><span class="sxs-lookup"><span data-stu-id="e39dd-341">(If you still have **Server Explorer** open from the earlier time, click the **Refresh** button.)</span></span>

![](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image14.png)

<span data-ttu-id="e39dd-342">您没有为该表创建模型类 `CourseInstructor` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-342">You didn't create a model class for the `CourseInstructor` table.</span></span> <span data-ttu-id="e39dd-343">如前所述，这是与实体之间的多对多关系的联接表 `Instructor` `Course` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-343">As explained earlier, this is a join table for the many-to-many relationship between the `Instructor` and `Course` entities.</span></span>

<span data-ttu-id="e39dd-344">右键单击 `CourseInstructor` 该表，然后选择 " **显示表数据** "，以验证它是否具有 `Instructor` 添加到导航属性的实体的结果中的数据 `Course.Instructors` 。</span><span class="sxs-lookup"><span data-stu-id="e39dd-344">Right-click the `CourseInstructor` table and select **Show Table Data** to verify that it has data in it as a result of the `Instructor` entities you added to the `Course.Instructors` navigation property.</span></span>

![Table_data_in_CourseInstructor_table](creating-a-more-complex-data-model-for-an-asp-net-mvc-application/_static/image15.png)

## <a name="summary"></a><span data-ttu-id="e39dd-346">总结</span><span class="sxs-lookup"><span data-stu-id="e39dd-346">Summary</span></span>

<span data-ttu-id="e39dd-347">现在你就得到了更复杂的数据模型和相应的数据库。</span><span class="sxs-lookup"><span data-stu-id="e39dd-347">You now have a more complex data model and corresponding database.</span></span> <span data-ttu-id="e39dd-348">在以下教程中，你将了解有关访问相关数据的不同方法的详细信息。</span><span class="sxs-lookup"><span data-stu-id="e39dd-348">In the following tutorial you'll learn more about different ways to access related data.</span></span>

<span data-ttu-id="e39dd-349">可在 [ASP.NET 数据访问内容映射](../../../../whitepapers/aspnet-data-access-content-map.md)中找到指向其他实体框架资源的链接。</span><span class="sxs-lookup"><span data-stu-id="e39dd-349">Links to other Entity Framework resources can be found in the [ASP.NET Data Access Content Map](../../../../whitepapers/aspnet-data-access-content-map.md).</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="e39dd-350">[上一页](sorting-filtering-and-paging-with-the-entity-framework-in-an-asp-net-mvc-application.md)
> [下一页](reading-related-data-with-the-entity-framework-in-an-asp-net-mvc-application.md)</span><span class="sxs-lookup"><span data-stu-id="e39dd-350">[Previous](sorting-filtering-and-paging-with-the-entity-framework-in-an-asp-net-mvc-application.md)
[Next](reading-related-data-with-the-entity-framework-in-an-asp-net-mvc-application.md)</span></span>
