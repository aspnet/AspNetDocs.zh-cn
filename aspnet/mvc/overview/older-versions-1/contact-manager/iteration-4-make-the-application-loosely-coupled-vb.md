---
uid: mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
title: 迭代#4 = 使应用程序松散耦合 （VB） |微软文档
author: rick-anderson
description: 在第四次迭代中，我们利用多种软件设计模式，使维护和修改联系人管理器应用程序变得更加容易。 预测...
ms.author: riande
ms.date: 02/20/2009
ms.assetid: 92c70297-4430-4e4e-919a-9c2333a8d09a
msc.legacyurl: /mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
msc.type: authoredcontent
ms.openlocfilehash: 1026e307b7e498a8b1392f2907c08cdcd05a3199
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542763"
---
# <a name="iteration-4--make-the-application-loosely-coupled-vb"></a><span data-ttu-id="d08da-104">迭代 4 – 使应用程序松散耦合 (VB)</span><span class="sxs-lookup"><span data-stu-id="d08da-104">Iteration #4 – Make the application loosely coupled (VB)</span></span>

<span data-ttu-id="d08da-105">由[微软](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="d08da-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="d08da-106">下载代码</span><span class="sxs-lookup"><span data-stu-id="d08da-106">Download Code</span></span>](iteration-4-make-the-application-loosely-coupled-vb/_static/contactmanager_4_vb1.zip)

> <span data-ttu-id="d08da-107">在第四次迭代中，我们利用多种软件设计模式，使维护和修改联系人管理器应用程序变得更加容易。</span><span class="sxs-lookup"><span data-stu-id="d08da-107">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="d08da-108">例如，我们重构应用程序以使用存储库模式和依赖项注入模式。</span><span class="sxs-lookup"><span data-stu-id="d08da-108">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

## <a name="building-a-contact-management-aspnet-mvc-application-vb"></a><span data-ttu-id="d08da-109">构建 MVC 应用程序 （VB） ASP.NET联系人管理</span><span class="sxs-lookup"><span data-stu-id="d08da-109">Building a Contact Management ASP.NET MVC Application (VB)</span></span>

<span data-ttu-id="d08da-110">在本系列教程中，我们从头到尾构建整个联系人管理应用程序。</span><span class="sxs-lookup"><span data-stu-id="d08da-110">In this series of tutorials, we build an entire Contact Management application from start to finish.</span></span> <span data-ttu-id="d08da-111">通过联系人管理器应用程序，您可以存储联系人信息 （姓名、电话号码和电子邮件地址） 人员列表。</span><span class="sxs-lookup"><span data-stu-id="d08da-111">The Contact Manager application enables you to store contact information - names, phone numbers and email addresses - for a list of people.</span></span>

<span data-ttu-id="d08da-112">我们通过多次迭代构建应用程序。</span><span class="sxs-lookup"><span data-stu-id="d08da-112">We build the application over multiple iterations.</span></span> <span data-ttu-id="d08da-113">每次迭代时，我们都会逐步改进应用程序。</span><span class="sxs-lookup"><span data-stu-id="d08da-113">With each iteration, we gradually improve the application.</span></span> <span data-ttu-id="d08da-114">此多迭代方法的目标是使您能够了解每次更改的原因。</span><span class="sxs-lookup"><span data-stu-id="d08da-114">The goal of this multiple iteration approach is to enable you to understand the reason for each change.</span></span>

- <span data-ttu-id="d08da-115">迭代#1 - 创建应用程序。</span><span class="sxs-lookup"><span data-stu-id="d08da-115">Iteration #1 - Create the application.</span></span> <span data-ttu-id="d08da-116">在第一次迭代中，我们以最简单的方式创建联系人管理器。</span><span class="sxs-lookup"><span data-stu-id="d08da-116">In the first iteration, we create the Contact Manager in the simplest way possible.</span></span> <span data-ttu-id="d08da-117">我们添加对基本数据库操作的支持：创建、读取、更新和删除 （CRUD）。</span><span class="sxs-lookup"><span data-stu-id="d08da-117">We add support for basic database operations: Create, Read, Update, and Delete (CRUD).</span></span>

- <span data-ttu-id="d08da-118">迭代#2 - 使应用程序看起来不错。</span><span class="sxs-lookup"><span data-stu-id="d08da-118">Iteration #2 - Make the application look nice.</span></span> <span data-ttu-id="d08da-119">在此迭代中，我们通过修改默认ASP.NET MVC 视图母版页和级联样式表来改进应用程序的外观。</span><span class="sxs-lookup"><span data-stu-id="d08da-119">In this iteration, we improve the appearance of the application by modifying the default ASP.NET MVC view master page and cascading style sheet.</span></span>

- <span data-ttu-id="d08da-120">迭代#3 - 添加表单验证。</span><span class="sxs-lookup"><span data-stu-id="d08da-120">Iteration #3 - Add form validation.</span></span> <span data-ttu-id="d08da-121">在第三个迭代中，我们添加基本表单验证。</span><span class="sxs-lookup"><span data-stu-id="d08da-121">In the third iteration, we add basic form validation.</span></span> <span data-ttu-id="d08da-122">我们防止人们在未填写所需表单字段的情况下提交表单。</span><span class="sxs-lookup"><span data-stu-id="d08da-122">We prevent people from submitting a form without completing required form fields.</span></span> <span data-ttu-id="d08da-123">我们还验证电子邮件地址和电话号码。</span><span class="sxs-lookup"><span data-stu-id="d08da-123">We also validate email addresses and phone numbers.</span></span>

- <span data-ttu-id="d08da-124">迭代#4 - 使应用程序松散耦合。</span><span class="sxs-lookup"><span data-stu-id="d08da-124">Iteration #4 - Make the application loosely coupled.</span></span> <span data-ttu-id="d08da-125">在第四次迭代中，我们利用多种软件设计模式，使维护和修改联系人管理器应用程序变得更加容易。</span><span class="sxs-lookup"><span data-stu-id="d08da-125">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="d08da-126">例如，我们重构应用程序以使用存储库模式和依赖项注入模式。</span><span class="sxs-lookup"><span data-stu-id="d08da-126">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

- <span data-ttu-id="d08da-127">迭代#5 - 创建单元测试。</span><span class="sxs-lookup"><span data-stu-id="d08da-127">Iteration #5 - Create unit tests.</span></span> <span data-ttu-id="d08da-128">在第五次迭代中，我们通过添加单元测试使应用程序更易于维护和修改。</span><span class="sxs-lookup"><span data-stu-id="d08da-128">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="d08da-129">我们模拟数据模型类，并为控制器和验证逻辑构建单元测试。</span><span class="sxs-lookup"><span data-stu-id="d08da-129">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>

- <span data-ttu-id="d08da-130">迭代#6 - 使用测试驱动开发。</span><span class="sxs-lookup"><span data-stu-id="d08da-130">Iteration #6 - Use test-driven development.</span></span> <span data-ttu-id="d08da-131">在第六次迭代中，我们首先编写单元测试并针对单元测试编写代码，从而向应用程序添加新功能。</span><span class="sxs-lookup"><span data-stu-id="d08da-131">In this sixth iteration, we add new functionality to our application by writing unit tests first and writing code against the unit tests.</span></span> <span data-ttu-id="d08da-132">在此迭代中，我们添加联系人组。</span><span class="sxs-lookup"><span data-stu-id="d08da-132">In this iteration, we add contact groups.</span></span>

- <span data-ttu-id="d08da-133">迭代#7 - 添加Ajax功能。</span><span class="sxs-lookup"><span data-stu-id="d08da-133">Iteration #7 - Add Ajax functionality.</span></span> <span data-ttu-id="d08da-134">在第七次迭代中，我们通过增加对Ajax的支持来提高应用程序的响应性和性能。</span><span class="sxs-lookup"><span data-stu-id="d08da-134">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="this-iteration"></a><span data-ttu-id="d08da-135">此迭代</span><span class="sxs-lookup"><span data-stu-id="d08da-135">This Iteration</span></span>

<span data-ttu-id="d08da-136">在 Contact Manager 应用程序的第四个迭代中，我们重构应用程序以使应用程序更松散耦合。</span><span class="sxs-lookup"><span data-stu-id="d08da-136">In this fourth iteration of the Contact Manager application, we refactor the application to make the application more loosely coupled.</span></span> <span data-ttu-id="d08da-137">当应用程序松散耦合时，可以在应用程序一个部分修改代码，而无需修改应用程序其他部分中的代码。</span><span class="sxs-lookup"><span data-stu-id="d08da-137">When an application is loosely coupled, you can modify the code in one part of the application without needing to modify the code in other parts of the application.</span></span> <span data-ttu-id="d08da-138">松散耦合的应用程序对更改更具弹性。</span><span class="sxs-lookup"><span data-stu-id="d08da-138">Loosely coupled applications are more resilient to change.</span></span>

<span data-ttu-id="d08da-139">目前，Contact Manager 应用程序使用的所有数据访问和验证逻辑都包含在控制器类中。</span><span class="sxs-lookup"><span data-stu-id="d08da-139">Currently, all of the data access and validation logic used by the Contact Manager application is contained in the controller classes.</span></span> <span data-ttu-id="d08da-140">这是个坏主意。</span><span class="sxs-lookup"><span data-stu-id="d08da-140">This is a bad idea.</span></span> <span data-ttu-id="d08da-141">每当需要修改应用程序的一个部分时，您都有可能将 Bug 引入应用程序的另一部分。</span><span class="sxs-lookup"><span data-stu-id="d08da-141">Whenever you need to modify one part of your application, you risk introducing bugs into another part of your application.</span></span> <span data-ttu-id="d08da-142">例如，如果修改验证逻辑，则有在数据访问或控制器逻辑中引入新 Bug 的风险。</span><span class="sxs-lookup"><span data-stu-id="d08da-142">For example, if you modify your validation logic, you risk introducing new bugs into your data access or controller logic.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="d08da-143">（SRP），类不应有超过一个更改的原因。</span><span class="sxs-lookup"><span data-stu-id="d08da-143">(SRP), a class should never have more than one reason to change.</span></span> <span data-ttu-id="d08da-144">混合控制器、验证和数据库逻辑严重违反了单一责任原则。</span><span class="sxs-lookup"><span data-stu-id="d08da-144">Mixing controller, validation, and database logic is a massive violation of the Single Responsibility Principle.</span></span>

<span data-ttu-id="d08da-145">可能需要修改应用程序的原因有多种。</span><span class="sxs-lookup"><span data-stu-id="d08da-145">There are several reasons that you might need to modify your application.</span></span> <span data-ttu-id="d08da-146">您可能需要向应用程序添加新功能，可能需要修复应用程序中的 Bug，或者可能需要修改应用程序的实现方式。</span><span class="sxs-lookup"><span data-stu-id="d08da-146">You might need to add a new feature to your application, you might need to fix a bug in your application, or you might need to modify how a feature of your application is implemented.</span></span> <span data-ttu-id="d08da-147">应用程序很少是静态的。</span><span class="sxs-lookup"><span data-stu-id="d08da-147">Applications are rarely static.</span></span> <span data-ttu-id="d08da-148">它们往往随着时间的推移而生长和变异。</span><span class="sxs-lookup"><span data-stu-id="d08da-148">They tend to grow and mutate over time.</span></span>

<span data-ttu-id="d08da-149">例如，假设您决定更改实现数据访问层的方式。</span><span class="sxs-lookup"><span data-stu-id="d08da-149">Imagine, for example, that you decide to change how you implement your data access layer.</span></span> <span data-ttu-id="d08da-150">现在，联系人管理器应用程序使用 Microsoft 实体框架访问数据库。</span><span class="sxs-lookup"><span data-stu-id="d08da-150">Right now, the Contact Manager application uses the Microsoft Entity Framework to access the database.</span></span> <span data-ttu-id="d08da-151">但是，您可能决定迁移到新的或替代的数据访问技术，如ADO.NET数据服务或 NHibernate。</span><span class="sxs-lookup"><span data-stu-id="d08da-151">However, you might decide to migrate to a new or alternative data access technology such as ADO.NET Data Services or NHibernate.</span></span> <span data-ttu-id="d08da-152">但是，由于数据访问代码不与验证和控制器代码隔离，因此如果不修改与数据访问没有直接关系的其他代码，就无法修改应用程序中的数据访问代码。</span><span class="sxs-lookup"><span data-stu-id="d08da-152">However, because the data access code is not isolated from the validation and controller code, there is no way to modify the data access code in your application without modifying other code that is not directly related to data access.</span></span>

<span data-ttu-id="d08da-153">另一方面，当应用程序耦合松散时，您可以对应用程序的一个部分进行更改，而无需接触应用程序的其他部分。</span><span class="sxs-lookup"><span data-stu-id="d08da-153">When an application is loosely coupled, on the other hand, you can make changes to one part of an application without touching other parts of an application.</span></span> <span data-ttu-id="d08da-154">例如，无需修改验证或控制器逻辑即可切换数据访问技术。</span><span class="sxs-lookup"><span data-stu-id="d08da-154">For example, you can switch data access technologies without modifying your validation or controller logic.</span></span>

<span data-ttu-id="d08da-155">在此迭代中，我们利用了多种软件设计模式，使我们能够将我们的 Contact Manager 应用程序重构为更松散耦合的应用程序。</span><span class="sxs-lookup"><span data-stu-id="d08da-155">In this iteration, we take advantage of several software design patterns that enable us to refactor our Contact Manager application into a more loosely coupled application.</span></span> <span data-ttu-id="d08da-156">完成后，联系人管理器不会执行以前未执行的任何操作。</span><span class="sxs-lookup"><span data-stu-id="d08da-156">When we are done, the Contact Manager won t do anything that it didn't do before.</span></span> <span data-ttu-id="d08da-157">但是，我们将来能够更轻松地更改应用程序。</span><span class="sxs-lookup"><span data-stu-id="d08da-157">However, we'll be able to change the application more easily in the future.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="d08da-158">重构是重写应用程序的过程，这样它不会丢失任何现有功能。</span><span class="sxs-lookup"><span data-stu-id="d08da-158">Refactoring is the process of rewriting an application in such a way that it does not lose any existing functionality.</span></span>

## <a name="using-the-repository-software-design-pattern"></a><span data-ttu-id="d08da-159">使用存储库软件设计模式</span><span class="sxs-lookup"><span data-stu-id="d08da-159">Using the Repository Software Design Pattern</span></span>

<span data-ttu-id="d08da-160">我们的第一个变化是利用称为存储库模式的软件设计模式。</span><span class="sxs-lookup"><span data-stu-id="d08da-160">Our first change is to take advantage of a software design pattern called the Repository pattern.</span></span> <span data-ttu-id="d08da-161">我们将使用存储库模式将数据访问代码与应用程序的其余部分隔离开来。</span><span class="sxs-lookup"><span data-stu-id="d08da-161">We'll use the Repository pattern to isolate our data access code from the rest of our application.</span></span>

<span data-ttu-id="d08da-162">实现存储库模式需要我们完成以下两个步骤：</span><span class="sxs-lookup"><span data-stu-id="d08da-162">Implementing the Repository pattern requires us to complete the following two steps:</span></span>

1. <span data-ttu-id="d08da-163">创建接口</span><span class="sxs-lookup"><span data-stu-id="d08da-163">Create an interface</span></span>
2. <span data-ttu-id="d08da-164">创建实现接口的具体类</span><span class="sxs-lookup"><span data-stu-id="d08da-164">Create a concrete class that implements the interface</span></span>

<span data-ttu-id="d08da-165">首先，我们需要创建一个接口来描述我们需要执行的所有数据访问方法。</span><span class="sxs-lookup"><span data-stu-id="d08da-165">First, we need to create an interface that describes all of the data access methods that we need to perform.</span></span> <span data-ttu-id="d08da-166">IContactManager存储库接口包含在清单1中。</span><span class="sxs-lookup"><span data-stu-id="d08da-166">The IContactManagerRepository interface is contained in Listing 1.</span></span> <span data-ttu-id="d08da-167">此界面描述了五种方法：创建联系人、删除联系人、编辑联系人、获取联系人和列表联系人（）。</span><span class="sxs-lookup"><span data-stu-id="d08da-167">This interface describes five methods: CreateContact(), DeleteContact(), EditContact(), GetContact, and ListContacts().</span></span>

<span data-ttu-id="d08da-168">**清单 1 - 型号\IContactManagerRepository.vb**</span><span class="sxs-lookup"><span data-stu-id="d08da-168">**Listing 1 - Models\IContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample1.vb)]

<span data-ttu-id="d08da-169">接下来，我们需要创建一个实现 IContactManagerRepository 接口的具体类。</span><span class="sxs-lookup"><span data-stu-id="d08da-169">Next, we need to create a concrete class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="d08da-170">由于我们使用 Microsoft 实体框架访问数据库，因此我们将创建一个名为实体联系人管理器存储库的新类。</span><span class="sxs-lookup"><span data-stu-id="d08da-170">Because we are using the Microsoft Entity Framework to access the database, we'll create a new class named EntityContactManagerRepository.</span></span> <span data-ttu-id="d08da-171">此类包含在清单 2 中。</span><span class="sxs-lookup"><span data-stu-id="d08da-171">This class is contained in Listing 2.</span></span>

<span data-ttu-id="d08da-172">**清单2 - 模型_实体联系人管理器存储库.vb**</span><span class="sxs-lookup"><span data-stu-id="d08da-172">**Listing 2 - Models\EntityContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample2.vb)]

<span data-ttu-id="d08da-173">请注意，实体联系人管理器存储库类实现了 IContactManager 存储库接口。</span><span class="sxs-lookup"><span data-stu-id="d08da-173">Notice that the EntityContactManagerRepository class implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="d08da-174">类实现该接口描述的所有五种方法。</span><span class="sxs-lookup"><span data-stu-id="d08da-174">The class implements all five of the methods described by that interface.</span></span>

<span data-ttu-id="d08da-175">您可能想知道为什么我们需要处理一个接口。</span><span class="sxs-lookup"><span data-stu-id="d08da-175">You might wonder why we need to bother with an interface.</span></span> <span data-ttu-id="d08da-176">为什么我们需要同时创建接口和实现它的类？</span><span class="sxs-lookup"><span data-stu-id="d08da-176">Why do we need to create both an interface and a class that implements it?</span></span>

<span data-ttu-id="d08da-177">除了一个例外，我们应用程序的其余部分将与接口而不是具体类进行交互。</span><span class="sxs-lookup"><span data-stu-id="d08da-177">With one exception, the remainder of our application will interact with the interface and not the concrete class.</span></span> <span data-ttu-id="d08da-178">我们将调用 IContactManagerRepository 接口公开的方法，而不是调用实体ContactManagerRepository类公开的方法。</span><span class="sxs-lookup"><span data-stu-id="d08da-178">Instead of calling the methods exposed by the EntityContactManagerRepository class, we'll call the methods exposed by the IContactManagerRepository interface.</span></span>

<span data-ttu-id="d08da-179">这样，我们可以使用新类实现接口，而无需修改应用程序的其余部分。</span><span class="sxs-lookup"><span data-stu-id="d08da-179">That way, we can implement the interface with a new class without needing to modify the remainder of our application.</span></span> <span data-ttu-id="d08da-180">例如，在未来某个日期，我们可能想要实现实现 IContactManagerRepository 接口的数据服务联系人管理器存储库类。</span><span class="sxs-lookup"><span data-stu-id="d08da-180">For example, at some future date, we might want to implement an DataServicesContactManagerRepository class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="d08da-181">DataServicesContactManagerManagerRepository 类可能使用ADO.NET数据服务来访问数据库而不是 Microsoft 实体框架。</span><span class="sxs-lookup"><span data-stu-id="d08da-181">The DataServicesContactManagerRepository class might use ADO.NET Data Services to access a database instead of the Microsoft Entity Framework.</span></span>

<span data-ttu-id="d08da-182">如果我们的应用程序代码是根据 IContactManagerRepository 接口而不是具体的 EntityContactManagerManagerRepository 类编程的，那么我们可以切换具体类，而无需修改我们代码的任何其余部分。</span><span class="sxs-lookup"><span data-stu-id="d08da-182">If our application code is programmed against the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class then we can switch concrete classes without modifying any of the rest of our code.</span></span> <span data-ttu-id="d08da-183">例如，我们可以从实体联系人管理器存储库类切换到 DataServicesContactManagerManagerRepository 类，而无需修改我们的数据访问或验证逻辑。</span><span class="sxs-lookup"><span data-stu-id="d08da-183">For example, we can switch from the EntityContactManagerRepository class to the DataServicesContactManagerRepository class without modifying our data access or validation logic.</span></span>

<span data-ttu-id="d08da-184">针对接口（抽象）而不是具体类进行编程使我们的应用程序更具弹性，从而对更改进行编程。</span><span class="sxs-lookup"><span data-stu-id="d08da-184">Programming against interfaces (abstractions) instead of concrete classes makes our application more resilient to change.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="d08da-185">您可以通过选择菜单选项"重构"，提取界面，从 Visual Studio 中的特定类快速创建界面。</span><span class="sxs-lookup"><span data-stu-id="d08da-185">You can quickly create an interface from a concrete class within Visual Studio by selecting the menu option Refactor, Extract Interface.</span></span> <span data-ttu-id="d08da-186">例如，您可以先创建实体联系人管理器存储库类，然后使用提取接口自动生成 IContactManagerRepository 接口。</span><span class="sxs-lookup"><span data-stu-id="d08da-186">For example, you can create the EntityContactManagerRepository class first and then use Extract Interface to generate the IContactManagerRepository interface automatically.</span></span>

## <a name="using-the-dependency-injection-software-design-pattern"></a><span data-ttu-id="d08da-187">使用依赖项注入软件设计模式</span><span class="sxs-lookup"><span data-stu-id="d08da-187">Using the Dependency Injection Software Design Pattern</span></span>

<span data-ttu-id="d08da-188">现在，我们已经将数据访问代码迁移到单独的存储库类，我们需要修改我们的联系人控制器才能使用此类。</span><span class="sxs-lookup"><span data-stu-id="d08da-188">Now that we have migrated our data access code to a separate Repository class, we need to modify our Contact controller to use this class.</span></span> <span data-ttu-id="d08da-189">我们将利用称为依赖注入的软件设计模式，在我们的控制器中使用存储库类。</span><span class="sxs-lookup"><span data-stu-id="d08da-189">We will take advantage of a software design pattern called Dependency Injection to use the Repository class in our controller.</span></span>

<span data-ttu-id="d08da-190">修改后的联系人控制器包含在清单 3 中。</span><span class="sxs-lookup"><span data-stu-id="d08da-190">The modified Contact controller is contained in Listing 3.</span></span>

<span data-ttu-id="d08da-191">**清单3 - 控制器\联系控制器.vb**</span><span class="sxs-lookup"><span data-stu-id="d08da-191">**Listing 3 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample3.vb)]

<span data-ttu-id="d08da-192">请注意，清单 3 中的联系人控制器有两个构造函数。</span><span class="sxs-lookup"><span data-stu-id="d08da-192">Notice that the Contact controller in Listing 3 has two constructors.</span></span> <span data-ttu-id="d08da-193">第一个构造函数将 IContactManagerRepository 接口的具体实例传递给第二个构造函数。</span><span class="sxs-lookup"><span data-stu-id="d08da-193">The first constructor passes a concrete instance of the IContactManagerRepository interface to the second constructor.</span></span> <span data-ttu-id="d08da-194">联系人控制器类使用*构造函数依赖项注入*。</span><span class="sxs-lookup"><span data-stu-id="d08da-194">The Contact controller class uses *Constructor Dependency Injection*.</span></span>

<span data-ttu-id="d08da-195">使用实体联系人管理器存储库类的唯一位置位于第一个构造函数中。</span><span class="sxs-lookup"><span data-stu-id="d08da-195">The one and only place that the EntityContactManagerRepository class is used is in the first constructor.</span></span> <span data-ttu-id="d08da-196">类的其余部分使用 IContactManagerRepository 接口，而不是具体的实体联系人管理器存储库类。</span><span class="sxs-lookup"><span data-stu-id="d08da-196">The remainder of the class uses the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class.</span></span>

<span data-ttu-id="d08da-197">这样，将来可以轻松切换 IContactManagerRepository 类的实现。</span><span class="sxs-lookup"><span data-stu-id="d08da-197">This makes it easy to switch implementations of the IContactManagerRepository class in the future.</span></span> <span data-ttu-id="d08da-198">如果要使用 DataServicesContact存储库类而不是实体联系人管理器存储库类，只需修改第一个构造函数。</span><span class="sxs-lookup"><span data-stu-id="d08da-198">If you want to use the DataServicesContactRepository class instead of the EntityContactManagerRepository class, just modify the first constructor.</span></span>

<span data-ttu-id="d08da-199">构造函数依赖项注入也使联系人控制器类非常可测试。</span><span class="sxs-lookup"><span data-stu-id="d08da-199">Constructor Dependency injection also makes the Contact controller class very testable.</span></span> <span data-ttu-id="d08da-200">在单元测试中，您可以通过通过 IContactManagerRepository 类的模拟实现来实例化联系人控制器。</span><span class="sxs-lookup"><span data-stu-id="d08da-200">In your unit tests, you can instantiate the Contact controller by passing a mock implementation of the IContactManagerRepository class.</span></span> <span data-ttu-id="d08da-201">在下一次迭代中，当我们为联系人管理器应用程序构建单元测试时，依赖项注入的此功能对我们非常重要。</span><span class="sxs-lookup"><span data-stu-id="d08da-201">This feature of Dependency Injection will be very important to us in the next iteration when we build unit tests for the Contact Manager application.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="d08da-202">如果要将 Contact 控制器类与 IContactManagerRepository 接口的特定实现完全分离，则可以利用支持依赖项注入的框架，如结构映射或 Microsoft 实体框架 （MEF）。</span><span class="sxs-lookup"><span data-stu-id="d08da-202">If you want to completely decouple the Contact controller class from a particular implementation of the IContactManagerRepository interface then you can take advantage of a framework that supports Dependency Injection such as StructureMap or the Microsoft Entity Framework (MEF).</span></span> <span data-ttu-id="d08da-203">通过利用依赖项注入框架，您无需在代码中引用具体类。</span><span class="sxs-lookup"><span data-stu-id="d08da-203">By taking advantage of a Dependency Injection framework, you never need to refer to a concrete class in your code.</span></span>

## <a name="creating-a-service-layer"></a><span data-ttu-id="d08da-204">创建服务层</span><span class="sxs-lookup"><span data-stu-id="d08da-204">Creating a Service Layer</span></span>

<span data-ttu-id="d08da-205">您可能已经注意到，我们的验证逻辑仍然与清单 3 中修改后的控制器类中的控制器逻辑混合在一起。</span><span class="sxs-lookup"><span data-stu-id="d08da-205">You might have noticed that our validation logic is still mixed up with our controller logic in the modified controller class in Listing 3.</span></span> <span data-ttu-id="d08da-206">出于隔离数据访问逻辑的一个个好主意，最好隔离我们的验证逻辑。</span><span class="sxs-lookup"><span data-stu-id="d08da-206">For the same reason that it is a good idea to isolate our data access logic, it is a good idea to isolate our validation logic.</span></span>

<span data-ttu-id="d08da-207">为了解决此问题，我们可以创建一个单独的[服务层](http://martinfowler.com/eaaCatalog/serviceLayer.html)。</span><span class="sxs-lookup"><span data-stu-id="d08da-207">To fix this problem, we can create a separate [service layer](http://martinfowler.com/eaaCatalog/serviceLayer.html).</span></span> <span data-ttu-id="d08da-208">服务层是一个单独的层，我们可以插入控制器和存储库类之间。</span><span class="sxs-lookup"><span data-stu-id="d08da-208">The service layer is a separate layer that we can insert between our controller and repository classes.</span></span> <span data-ttu-id="d08da-209">服务层包含我们的业务逻辑，包括我们所有的验证逻辑。</span><span class="sxs-lookup"><span data-stu-id="d08da-209">The service layer contains our business logic including all of our validation logic.</span></span>

<span data-ttu-id="d08da-210">联系人管理器服务包含在清单 4 中。</span><span class="sxs-lookup"><span data-stu-id="d08da-210">The ContactManagerService is contained in Listing 4.</span></span> <span data-ttu-id="d08da-211">它包含来自联系人控制器类的验证逻辑。</span><span class="sxs-lookup"><span data-stu-id="d08da-211">It contains the validation logic from the Contact controller class.</span></span>

<span data-ttu-id="d08da-212">**清单4 - 型号_联系经理服务.vb**</span><span class="sxs-lookup"><span data-stu-id="d08da-212">**Listing 4 - Models\ContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample4.vb)]

<span data-ttu-id="d08da-213">请注意，联系人管理器服务的构造函数需要验证字典。</span><span class="sxs-lookup"><span data-stu-id="d08da-213">Notice that the constructor for the ContactManagerService requires a ValidationDictionary.</span></span> <span data-ttu-id="d08da-214">服务层通过此验证字典与控制器层通信。</span><span class="sxs-lookup"><span data-stu-id="d08da-214">The service layer communicates with the controller layer through this ValidationDictionary.</span></span> <span data-ttu-id="d08da-215">在讨论修饰器模式时，我们将在下一节中详细讨论验证词典。</span><span class="sxs-lookup"><span data-stu-id="d08da-215">We discuss the ValidationDictionary in detail in the following section when we discuss the Decorator pattern.</span></span>

<span data-ttu-id="d08da-216">此外，请注意，联系人管理器服务实现了 IContactManager 服务接口。</span><span class="sxs-lookup"><span data-stu-id="d08da-216">Notice, furthermore, that the ContactManagerService implements the IContactManagerService interface.</span></span> <span data-ttu-id="d08da-217">您应该始终努力针对接口而不是具体类进行编程。</span><span class="sxs-lookup"><span data-stu-id="d08da-217">You should always strive to program against interfaces instead of concrete classes.</span></span> <span data-ttu-id="d08da-218">联系人管理器应用程序中的其他类不直接与联系人管理器服务类交互。</span><span class="sxs-lookup"><span data-stu-id="d08da-218">Other classes in the Contact Manager application do not interact with the ContactManagerService class directly.</span></span> <span data-ttu-id="d08da-219">相反，除了一个例外，联系人管理器应用程序的其余部分根据 IContactManager 服务接口进行编程。</span><span class="sxs-lookup"><span data-stu-id="d08da-219">Instead, with one exception, the remainder of the Contact Manager application is programmed against the IContactManagerService interface.</span></span>

<span data-ttu-id="d08da-220">IContactManager 服务接口包含在清单 5 中。</span><span class="sxs-lookup"><span data-stu-id="d08da-220">The IContactManagerService interface is contained in Listing 5.</span></span>

<span data-ttu-id="d08da-221">**清单5 - 型号_IContactManager服务.vb**</span><span class="sxs-lookup"><span data-stu-id="d08da-221">**Listing 5 - Models\IContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample5.vb)]

<span data-ttu-id="d08da-222">修改后的联系人控制器类包含在清单 6 中。</span><span class="sxs-lookup"><span data-stu-id="d08da-222">The modified Contact controller class is contained in Listing 6.</span></span> <span data-ttu-id="d08da-223">请注意，联系人控制器不再与联系人管理器存储库进行交互。</span><span class="sxs-lookup"><span data-stu-id="d08da-223">Notice that the Contact controller no longer interacts with the ContactManager repository.</span></span> <span data-ttu-id="d08da-224">相反，联系人控制器与联系人管理器服务进行交互。</span><span class="sxs-lookup"><span data-stu-id="d08da-224">Instead, the Contact controller interacts with the ContactManager service.</span></span> <span data-ttu-id="d08da-225">每个图层都尽可能与其他图层隔离。</span><span class="sxs-lookup"><span data-stu-id="d08da-225">Each layer is isolated as much as possible from other layers.</span></span>

<span data-ttu-id="d08da-226">**清单6 - 控制器\联系控制器.vb**</span><span class="sxs-lookup"><span data-stu-id="d08da-226">**Listing 6 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample6.vb)]

<span data-ttu-id="d08da-227">我们的应用程序不再违反单一责任原则 （SRP）。</span><span class="sxs-lookup"><span data-stu-id="d08da-227">Our application no longer runs afoul of the Single Responsibility Principle (SRP).</span></span> <span data-ttu-id="d08da-228">清单6中的联系人控制器除控制应用程序执行流外，已被剥夺了所有责任。</span><span class="sxs-lookup"><span data-stu-id="d08da-228">The Contact controller in Listing 6 has been stripped of every responsibility other than controlling the flow of application execution.</span></span> <span data-ttu-id="d08da-229">所有验证逻辑都已从"联系人"控制器中删除，并推送到服务层。</span><span class="sxs-lookup"><span data-stu-id="d08da-229">All the validation logic has been removed from the Contact controller and pushed into the service layer.</span></span> <span data-ttu-id="d08da-230">所有数据库逻辑都已推送到存储库层。</span><span class="sxs-lookup"><span data-stu-id="d08da-230">All of the database logic has been pushed into the repository layer.</span></span>

## <a name="using-the-decorator-pattern"></a><span data-ttu-id="d08da-231">使用修饰器模式</span><span class="sxs-lookup"><span data-stu-id="d08da-231">Using the Decorator Pattern</span></span>

<span data-ttu-id="d08da-232">我们希望能够将服务层与控制器层完全分离。</span><span class="sxs-lookup"><span data-stu-id="d08da-232">We want to be able to completely decouple our service layer from our controller layer.</span></span> <span data-ttu-id="d08da-233">原则上，我们应该能够在与控制器层分开的程序集中编译我们的服务层，而无需添加对 MVC 应用程序的引用。</span><span class="sxs-lookup"><span data-stu-id="d08da-233">In principle, we should be able to compile our service layer in a separate assembly from our controller layer without needing to add a reference to our MVC application.</span></span>

<span data-ttu-id="d08da-234">但是，我们的服务层需要能够将验证错误消息传递回控制器层。</span><span class="sxs-lookup"><span data-stu-id="d08da-234">However, our service layer needs to be able to pass validation error messages back to the controller layer.</span></span> <span data-ttu-id="d08da-235">我们如何使服务层在不耦合控制器和服务层的情况下通信验证错误消息？</span><span class="sxs-lookup"><span data-stu-id="d08da-235">How can we enable the service layer to communicate validation error messages without coupling the controller and service layer?</span></span> <span data-ttu-id="d08da-236">我们可以利用一个软件设计模式，称为[装饰模式](http://en.wikipedia.org/wiki/Decorator_pattern)。</span><span class="sxs-lookup"><span data-stu-id="d08da-236">We can take advantage of a software design pattern named the [Decorator pattern](http://en.wikipedia.org/wiki/Decorator_pattern).</span></span>

<span data-ttu-id="d08da-237">控制器使用名为 ModelState 的 ModelState 字典来表示验证错误。</span><span class="sxs-lookup"><span data-stu-id="d08da-237">A controller uses a ModelStateDictionary named ModelState to represent validation errors.</span></span> <span data-ttu-id="d08da-238">因此，您可能受诱惑将 ModelState 从控制器层传递到服务层。</span><span class="sxs-lookup"><span data-stu-id="d08da-238">Therefore, you might be tempted to pass ModelState from the controller layer to the service layer.</span></span> <span data-ttu-id="d08da-239">但是，在服务层中使用 ModelState 将使服务层依赖于ASP.NET MVC 框架的功能。</span><span class="sxs-lookup"><span data-stu-id="d08da-239">However, using ModelState in the service layer would make your service layer dependent on a feature of the ASP.NET MVC framework.</span></span> <span data-ttu-id="d08da-240">这将很糟糕，因为有一天，您可能希望将服务层与 WPF 应用程序一起使用，而不是使用ASP.NET MVC 应用程序。</span><span class="sxs-lookup"><span data-stu-id="d08da-240">This would be bad because, someday, you might want to use the service layer with a WPF application instead of an ASP.NET MVC application.</span></span> <span data-ttu-id="d08da-241">在这种情况下，您不希望引用ASP.NET MVC 框架来使用 ModelState字典类。</span><span class="sxs-lookup"><span data-stu-id="d08da-241">In that case, you wouldn't want to reference the ASP.NET MVC framework to use the ModelStateDictionary class.</span></span>

<span data-ttu-id="d08da-242">修饰器模式使您能够在新类中包装现有类，以实现接口。</span><span class="sxs-lookup"><span data-stu-id="d08da-242">The Decorator pattern enables you to wrap an existing class in a new class in order to implement an interface.</span></span> <span data-ttu-id="d08da-243">我们的联系人管理器项目包括清单 7 中包含的 ModelStateWrapper 类。</span><span class="sxs-lookup"><span data-stu-id="d08da-243">Our Contact Manager project includes the ModelStateWrapper class contained in Listing 7.</span></span> <span data-ttu-id="d08da-244">ModelStateWrapper 类实现清单 8 中的接口。</span><span class="sxs-lookup"><span data-stu-id="d08da-244">The ModelStateWrapper class implements the interface in Listing 8.</span></span>

<span data-ttu-id="d08da-245">**清单7 - 模型_验证_模型状态包装.vb**</span><span class="sxs-lookup"><span data-stu-id="d08da-245">**Listing 7 - Models\Validation\ModelStateWrapper.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample7.vb)]

<span data-ttu-id="d08da-246">**清单8 - 模型_验证_验证字典.vb**</span><span class="sxs-lookup"><span data-stu-id="d08da-246">**Listing 8 - Models\Validation\IValidationDictionary.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample8.vb)]

<span data-ttu-id="d08da-247">如果您仔细查看清单 5，您将看到 ContactManager 服务层专用 IValidationa 字典界面。</span><span class="sxs-lookup"><span data-stu-id="d08da-247">If you take a close look at Listing 5 then you'll see that the ContactManager service layer uses the IValidationDictionary interface exclusively.</span></span> <span data-ttu-id="d08da-248">联系人管理器服务不依赖于 ModelState 字典类。</span><span class="sxs-lookup"><span data-stu-id="d08da-248">The ContactManager service is not dependent on the ModelStateDictionary class.</span></span> <span data-ttu-id="d08da-249">当联系人控制器创建 ContactManager 服务时，控制器将像这样包装其 ModelState：</span><span class="sxs-lookup"><span data-stu-id="d08da-249">When the Contact controller creates the ContactManager service, the controller wraps its ModelState like this:</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample9.vb)]

## <a name="summary"></a><span data-ttu-id="d08da-250">总结</span><span class="sxs-lookup"><span data-stu-id="d08da-250">Summary</span></span>

<span data-ttu-id="d08da-251">在此迭代中，我们没有向联系人管理器应用程序添加任何新功能。</span><span class="sxs-lookup"><span data-stu-id="d08da-251">In this iteration, we did not add any new functionality to the Contact Manager application.</span></span> <span data-ttu-id="d08da-252">此迭代的目标是重构联系人管理器应用程序，以便更易于维护和修改。</span><span class="sxs-lookup"><span data-stu-id="d08da-252">The goal of this iteration was to refactor the Contact Manager application so that is easier to maintain and modify.</span></span>

<span data-ttu-id="d08da-253">首先，我们实现了存储库软件设计模式。</span><span class="sxs-lookup"><span data-stu-id="d08da-253">First, we implemented the Repository software design pattern.</span></span> <span data-ttu-id="d08da-254">我们将所有数据访问代码迁移到单独的 ContactManager 存储库类。</span><span class="sxs-lookup"><span data-stu-id="d08da-254">We migrated all of the data access code to a separate ContactManager repository class.</span></span>

<span data-ttu-id="d08da-255">我们还将验证逻辑与控制器逻辑隔离开来。</span><span class="sxs-lookup"><span data-stu-id="d08da-255">We also isolated our validation logic from our controller logic.</span></span> <span data-ttu-id="d08da-256">我们创建了一个单独的服务层，其中包含我们所有的验证代码。</span><span class="sxs-lookup"><span data-stu-id="d08da-256">We created a separate service layer that contains all of our validation code.</span></span> <span data-ttu-id="d08da-257">控制器层与服务层交互，服务层与存储库层交互。</span><span class="sxs-lookup"><span data-stu-id="d08da-257">The controller layer interacts with the service layer, and the service layer interacts with the repository layer.</span></span>

<span data-ttu-id="d08da-258">创建服务层时，我们利用修饰器模式将 ModelState 与服务层隔离开来。</span><span class="sxs-lookup"><span data-stu-id="d08da-258">When we created the service layer, we took advantage of the Decorator pattern to isolate ModelState from our service layer.</span></span> <span data-ttu-id="d08da-259">在我们的服务层中，我们根据 I验证词典接口而不是 ModelState 进行了编程。</span><span class="sxs-lookup"><span data-stu-id="d08da-259">In our service layer, we programmed against the IValidationDictionary interface instead of ModelState.</span></span>

<span data-ttu-id="d08da-260">最后，我们利用了名为依赖项注入模式的软件设计模式。</span><span class="sxs-lookup"><span data-stu-id="d08da-260">Finally, we took advantage of a software design pattern named the Dependency Injection pattern.</span></span> <span data-ttu-id="d08da-261">此模式使我们能够针对接口（抽象）而不是具体类进行编程。</span><span class="sxs-lookup"><span data-stu-id="d08da-261">This pattern enables us to program against interfaces (abstractions) instead of concrete classes.</span></span> <span data-ttu-id="d08da-262">实现依赖项注入设计模式也使我们的代码更具可测试性。</span><span class="sxs-lookup"><span data-stu-id="d08da-262">Implementing the Dependency Injection design pattern also makes our code more testable.</span></span> <span data-ttu-id="d08da-263">在下一个迭代中，我们将单元测试添加到项目中。</span><span class="sxs-lookup"><span data-stu-id="d08da-263">In the next iteration, we add unit tests to our project.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="d08da-264">[上一页](iteration-3-add-form-validation-vb.md)
> [下一页](iteration-5-create-unit-tests-vb.md)</span><span class="sxs-lookup"><span data-stu-id="d08da-264">[Previous](iteration-3-add-form-validation-vb.md)
[Next](iteration-5-create-unit-tests-vb.md)</span></span>
