---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: 在图表中显示数据，其中ASP.NET网页 （Razor） |微软文档
author: rick-anderson
description: 本章介绍如何在图表中显示数据。 在前面的章节中，您学习了如何在网格中手动显示数据。 本章解释...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: ad55d4898ee39e0239ef8b0bd5eea6387af3c816
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542880"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="65b55-105">在图表中显示具有ASP.NET网页（Razor）的数据</span><span class="sxs-lookup"><span data-stu-id="65b55-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="65b55-106">由[微软](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="65b55-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="65b55-107">本文介绍如何使用图表使用`Chart`帮助程序在ASP.NET网页 （Razor） 网站中显示数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="65b55-108">**您将学习：**</span><span class="sxs-lookup"><span data-stu-id="65b55-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="65b55-109">如何在图表中显示数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="65b55-110">如何使用内置主题对图表进行样式设置。</span><span class="sxs-lookup"><span data-stu-id="65b55-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="65b55-111">如何保存图表以及如何缓存图表以获得更好的性能。</span><span class="sxs-lookup"><span data-stu-id="65b55-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="65b55-112">以下是本文介绍的ASP.NET编程功能：</span><span class="sxs-lookup"><span data-stu-id="65b55-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="65b55-113">帮手`Chart`</span><span class="sxs-lookup"><span data-stu-id="65b55-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="65b55-114">本文中的信息适用于ASP.NET网页 1.0 和网页 2。</span><span class="sxs-lookup"><span data-stu-id="65b55-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="65b55-115">图表帮助程序</span><span class="sxs-lookup"><span data-stu-id="65b55-115">The Chart Helper</span></span>

<span data-ttu-id="65b55-116">当您要以图形形式显示数据时，可以使用`Chart`帮助程序。</span><span class="sxs-lookup"><span data-stu-id="65b55-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="65b55-117">`Chart`帮助程序可以渲染显示各种图表类型中的数据的图像。</span><span class="sxs-lookup"><span data-stu-id="65b55-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="65b55-118">它支持许多格式设置和标签选项。</span><span class="sxs-lookup"><span data-stu-id="65b55-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="65b55-119">`Chart`帮助程序可以呈现 30 多种图表类型，包括您可能从 Microsoft Excel 或其他工具&#8212;区域图、条形图、柱形图、折线图和饼图等工具中熟悉的所有类型的图表，以及更专业的图表（如股票图表）。</span><span class="sxs-lookup"><span data-stu-id="65b55-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="65b55-120">**面积图**![描述：面积图类型的图片](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="65b55-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="65b55-121">**条形图**![说明：条形图类型的图片](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="65b55-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="65b55-122">**柱形图**![说明：柱形图类型的图片](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="65b55-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="65b55-123">**折线图**![描述：折线图类型的图片](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="65b55-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="65b55-124">**饼图**![说明：饼图类型的图片](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="65b55-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="65b55-125">**股票图表**![描述：股票图表类型的图片](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="65b55-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="65b55-126">图表元素</span><span class="sxs-lookup"><span data-stu-id="65b55-126">Chart Elements</span></span>

<span data-ttu-id="65b55-127">图表显示数据和其他元素，如图例、轴、系列等。</span><span class="sxs-lookup"><span data-stu-id="65b55-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="65b55-128">下图显示了使用`Chart`帮助器时可以自定义的许多图表元素。</span><span class="sxs-lookup"><span data-stu-id="65b55-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="65b55-129">本文介绍如何设置其中某些（不是全部）元素。</span><span class="sxs-lookup"><span data-stu-id="65b55-129">This article shows you how to set some (not all) of these elements.</span></span>

![说明：显示图表元素的图片](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="65b55-131">从数据创建图表</span><span class="sxs-lookup"><span data-stu-id="65b55-131">Creating a Chart from Data</span></span>

<span data-ttu-id="65b55-132">在图表中显示的数据可以来自数组、从数据库返回的结果或 XML 文件中的数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="65b55-133">使用数组</span><span class="sxs-lookup"><span data-stu-id="65b55-133">Using an Array</span></span>

<span data-ttu-id="65b55-134">如ASP.NET[网页编程简介中所述，使用 Razor 语法](https://go.microsoft.com/fwlink/?LinkId=202890)编程，数组允许您在单个变量中存储类似项的集合。</span><span class="sxs-lookup"><span data-stu-id="65b55-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="65b55-135">可以使用数组来包含要包含在图表中的数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="65b55-136">此过程演示如何使用默认图表类型从数组中的数据创建图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="65b55-137">它还演示如何在页面中显示图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="65b55-138">创建名为*ChartArrayBasic.cshtml*的新文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="65b55-139">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="65b55-140">代码首先创建一个新图表并设置其宽度和高度。</span><span class="sxs-lookup"><span data-stu-id="65b55-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="65b55-141">使用`AddTitle`方法指定图表标题。</span><span class="sxs-lookup"><span data-stu-id="65b55-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="65b55-142">要添加数据，请使用 方法`AddSeries`。</span><span class="sxs-lookup"><span data-stu-id="65b55-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="65b55-143">在此示例中`name`，使用`xValue``yValues``AddSeries`方法 的 和 参数。</span><span class="sxs-lookup"><span data-stu-id="65b55-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="65b55-144">参数`name`显示在图表图例中。</span><span class="sxs-lookup"><span data-stu-id="65b55-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="65b55-145">该`xValue`参数包含沿图表水平轴显示的数据数组。</span><span class="sxs-lookup"><span data-stu-id="65b55-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="65b55-146">该`yValues`参数包含用于绘制图表垂直点的数据数组。</span><span class="sxs-lookup"><span data-stu-id="65b55-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="65b55-147">该方法`Write`实际上呈现图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="65b55-148">在这种情况下，由于您没有指定图表类型，`Chart`帮助程序将呈现其默认图表，即柱形图。</span><span class="sxs-lookup"><span data-stu-id="65b55-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="65b55-149">在浏览器中运行页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-149">Run the page in the browser.</span></span> <span data-ttu-id="65b55-150">浏览器显示图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="65b55-151">对图表数据使用数据库查询</span><span class="sxs-lookup"><span data-stu-id="65b55-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="65b55-152">如果要绘制图表的信息位于数据库中，则可以运行数据库查询，然后使用结果中的数据创建图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="65b55-153">此过程演示如何读取和显示在[文章"使用ASP.NET网页网站中的数据库简介](https://go.microsoft.com/fwlink/?LinkId=202893)"中创建的数据库中的数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="65b55-154">如果该文件夹不存在，则向网站的根目录添加*应用\_数据*文件夹。</span><span class="sxs-lookup"><span data-stu-id="65b55-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="65b55-155">在 *"应用\_数据*"文件夹中，添加名为*SmallBakery.sdf*的数据库文件，该文件在["使用ASP.NET网页网站中的数据库简介"中](https://go.microsoft.com/fwlink/?LinkId=202893)所述。</span><span class="sxs-lookup"><span data-stu-id="65b55-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="65b55-156">创建名为*ChartDataQuery.cshtml*的新文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="65b55-157">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="65b55-158">代码首先打开 SmallBakery 数据库并将其分配给名为 的`db`变量。</span><span class="sxs-lookup"><span data-stu-id="65b55-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="65b55-159">此变量表示可用于`Database`读取数据库和写入数据库的对象。</span><span class="sxs-lookup"><span data-stu-id="65b55-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="65b55-160">接下来，代码运行 SQL 查询以获取每个产品的名称和价格。</span><span class="sxs-lookup"><span data-stu-id="65b55-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="65b55-161">代码创建一个新图表，并通过调用图表`DataBindTable`的方法将数据库查询传递给它。</span><span class="sxs-lookup"><span data-stu-id="65b55-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="65b55-162">此方法采用两个参数：`dataSource`参数用于查询中的数据，`xField`该参数允许您设置用于图表 x 轴的数据列。</span><span class="sxs-lookup"><span data-stu-id="65b55-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="65b55-163">作为使用 方法的替代方法`DataBindTable`，可以使用`AddSeries``Chart`帮助器的方法。</span><span class="sxs-lookup"><span data-stu-id="65b55-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="65b55-164">该方法`AddSeries`允许您设置 和`xValue``yValues`参数。</span><span class="sxs-lookup"><span data-stu-id="65b55-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="65b55-165">例如，而不是使用如下所示`DataBindTable`的方法：</span><span class="sxs-lookup"><span data-stu-id="65b55-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="65b55-166">可以使用如下所示`AddSeries`的方法：</span><span class="sxs-lookup"><span data-stu-id="65b55-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="65b55-167">两者都呈现相同的结果。</span><span class="sxs-lookup"><span data-stu-id="65b55-167">Both render the same results.</span></span> <span data-ttu-id="65b55-168">该方法`AddSeries`更灵活，因为您可以更明确地指定图表类型和数据，但如果不需要额外的灵活性，`DataBindTable`则该方法更易于使用。</span><span class="sxs-lookup"><span data-stu-id="65b55-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="65b55-169">在浏览器中运行页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="65b55-170">使用 XML 数据</span><span class="sxs-lookup"><span data-stu-id="65b55-170">Using XML Data</span></span>

<span data-ttu-id="65b55-171">图表的第三个选项是使用 XML 文件作为图表的数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="65b55-172">这要求 XML 文件也具有描述 XML 结构的架构文件 *（.xsd*文件）。</span><span class="sxs-lookup"><span data-stu-id="65b55-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="65b55-173">此过程演示如何从 XML 文件读取数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="65b55-174">在*应用\_数据*文件夹中，创建名为*data.xml*的新 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="65b55-175">将现有 XML 替换为以下内容，即有关虚构公司员工的一些 XML 数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="65b55-176">在*应用\_数据*文件夹中，创建名为*data.xsd*的新 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="65b55-177">（请注意，这次的扩展是 *.xsd*.）</span><span class="sxs-lookup"><span data-stu-id="65b55-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="65b55-178">将现有 XML 替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="65b55-179">在网站的根目录中，创建一个名为*ChartDataXML.cshtml*的新文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="65b55-180">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="65b55-181">代码首先创建一个`DataSet`对象。</span><span class="sxs-lookup"><span data-stu-id="65b55-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="65b55-182">此对象用于管理从 XML 文件读取的数据，并根据架构文件中的信息对其进行组织。</span><span class="sxs-lookup"><span data-stu-id="65b55-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="65b55-183">（请注意，代码的顶部包括 语句`using SystemData`。</span><span class="sxs-lookup"><span data-stu-id="65b55-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="65b55-184">这是为了能够处理`DataSet`对象所必需的。</span><span class="sxs-lookup"><span data-stu-id="65b55-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="65b55-185">有关详细信息，请参阅[&quot;本文后面的使用&quot;语句和完全限定名称](#SB_UsingStatements)。</span><span class="sxs-lookup"><span data-stu-id="65b55-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="65b55-186">接下来，代码基于数据集创建`DataView`一个对象。</span><span class="sxs-lookup"><span data-stu-id="65b55-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="65b55-187">数据视图提供图表可以绑定到&#8212;的对象，即读取和绘图。</span><span class="sxs-lookup"><span data-stu-id="65b55-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="65b55-188">`AddSeries`图表使用 方法绑定到数据，如您前面在绘制数组数据图表时所看到的那样，但此`xValue`时间和`yValues`参数设置为`DataView`对象除外。</span><span class="sxs-lookup"><span data-stu-id="65b55-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="65b55-189">此示例还演示如何指定特定的图表类型。</span><span class="sxs-lookup"><span data-stu-id="65b55-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="65b55-190">在`AddSeries`方法中添加数据时，`chartType`参数也会设置为显示饼图。</span><span class="sxs-lookup"><span data-stu-id="65b55-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="65b55-191">在浏览器中运行页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="65b55-192">"使用"语句和完全限定的名称</span><span class="sxs-lookup"><span data-stu-id="65b55-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="65b55-193">使用 Razor 语法ASP.NET网页的 .NET 框架由数千个组件（类）组成。</span><span class="sxs-lookup"><span data-stu-id="65b55-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="65b55-194">为了使使用所有这些类可以管理，它们被组织成*命名空间*，有点像库。</span><span class="sxs-lookup"><span data-stu-id="65b55-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="65b55-195">例如，`System.Web`命名空间包含支持浏览器/服务器通信的类，`System.Xml`命名空间包含用于创建和读取 XML 文件的类，`System.Data`并且命名空间包含允许您处理数据的类。</span><span class="sxs-lookup"><span data-stu-id="65b55-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="65b55-196">为了访问 .NET Framework 中的任何给定类，代码不仅需要了解类名称，还需要了解类中的命名空间。</span><span class="sxs-lookup"><span data-stu-id="65b55-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="65b55-197">例如，为了使用`Chart`帮助程序，代码需要查找`System.Web.Helpers.Chart`类，该类将命名空间 （）`System.Web.Helpers`与类名称 （ （ ）`Chart`合并在一起。</span><span class="sxs-lookup"><span data-stu-id="65b55-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="65b55-198">这称为类的*完全限定*名称&#8212;其在 .NET 框架的庞大范围内的完整、明确位置。</span><span class="sxs-lookup"><span data-stu-id="65b55-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="65b55-199">在代码中，如下所示：</span><span class="sxs-lookup"><span data-stu-id="65b55-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="65b55-200">但是，每次要引用类或帮助程序时，必须使用这些长而完全限定的名称是繁琐的（且容易出错）。</span><span class="sxs-lookup"><span data-stu-id="65b55-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="65b55-201">因此，为了更轻松地使用类名，可以*导入*您感兴趣的命名空间，这通常只是 .NET 框架中许多命名空间中的少数。</span><span class="sxs-lookup"><span data-stu-id="65b55-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="65b55-202">如果已导入命名空间，则只能使用类名称 （`Chart`） 而不是完全限定的名称 （`System.Web.Helpers.Chart`。</span><span class="sxs-lookup"><span data-stu-id="65b55-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="65b55-203">当代码运行并遇到类名称时，它可以只查看已导入的命名空间来查找该类。</span><span class="sxs-lookup"><span data-stu-id="65b55-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="65b55-204">当您使用带有 Razor 语法的ASP.NET网页创建网页时，您通常每次都使用相同的类集，包括`WebPage`类、各种帮助器等。</span><span class="sxs-lookup"><span data-stu-id="65b55-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="65b55-205">为了节省您每次创建网站时导入相关命名空间的工作，ASP.NET进行了配置，以便自动为每个网站导入一组核心命名空间。</span><span class="sxs-lookup"><span data-stu-id="65b55-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="65b55-206">这就是为什么到目前为止您不必处理命名空间或导入名称空间的原因;您使用的所有类都位于已导入的命名空间中。</span><span class="sxs-lookup"><span data-stu-id="65b55-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="65b55-207">但是，有时您需要使用不在自动导入的命名空间中的类。</span><span class="sxs-lookup"><span data-stu-id="65b55-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="65b55-208">在这种情况下，可以使用该类的完全限定名称，也可以手动导入包含该类的命名空间。</span><span class="sxs-lookup"><span data-stu-id="65b55-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="65b55-209">要导入命名空间，请使用`using`语句（`import`在 Visual Basic 中），如您前面的示例一样。</span><span class="sxs-lookup"><span data-stu-id="65b55-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="65b55-210">例如，`DataSet`类位于命名空间中`System.Data`。</span><span class="sxs-lookup"><span data-stu-id="65b55-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="65b55-211">命名`System.Data`空间不能自动可用于ASP.NET Razor 页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="65b55-212">因此，要使用完全限定`DataSet`名称使用类，可以使用如下所示的代码：</span><span class="sxs-lookup"><span data-stu-id="65b55-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="65b55-213">如果必须重复使用类，`DataSet`则可以导入如下所示的命名空间，然后在代码中仅使用类名称：</span><span class="sxs-lookup"><span data-stu-id="65b55-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="65b55-214">可以为要引用`using`的任何其他 .NET 框架命名空间添加语句。</span><span class="sxs-lookup"><span data-stu-id="65b55-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="65b55-215">但是，如上所述，您不需要经常执行此操作，因为要使用的大多数类都位于命名空间中，名称空间由ASP.NET自动导入，用于 *.cshtml*和 *.vbhtml*页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="65b55-216">在网页内显示图表</span><span class="sxs-lookup"><span data-stu-id="65b55-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="65b55-217">在到目前为止看到的示例中，您将创建一个图表，然后将图表作为图形直接呈现给浏览器。</span><span class="sxs-lookup"><span data-stu-id="65b55-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="65b55-218">但是，在许多情况下，您希望将图表显示为页面的一部分，而不仅仅是在浏览器中。</span><span class="sxs-lookup"><span data-stu-id="65b55-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="65b55-219">为此，需要两步过程。</span><span class="sxs-lookup"><span data-stu-id="65b55-219">To do that requires a two-step process.</span></span> <span data-ttu-id="65b55-220">第一步是创建生成图表的页面，正如您已经看到的那样。</span><span class="sxs-lookup"><span data-stu-id="65b55-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="65b55-221">第二步是在另一页中显示生成的图像。</span><span class="sxs-lookup"><span data-stu-id="65b55-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="65b55-222">要显示图像，请使用 HTML`<img>`元素，就像显示任何图像一样。</span><span class="sxs-lookup"><span data-stu-id="65b55-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="65b55-223">但是`<img>`，该元素引用的不是 *.jpg*或 *.png*文件，而是引用包含创建图表`Chart`的帮助器的 *.cshtml*文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="65b55-224">当显示页运行时，`<img>`元素获取帮助器的`Chart`输出并呈现图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="65b55-225">创建名为*ShowChart.cshtml*的文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="65b55-226">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="65b55-227">代码使用 元素`<img>`来显示您在*ChartArrayBasic.cshtml*文件中创建的图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="65b55-228">在浏览器中运行网页。</span><span class="sxs-lookup"><span data-stu-id="65b55-228">Run the web page in a browser.</span></span> <span data-ttu-id="65b55-229">*ShowChart.cshtml*文件基于*ChartArrayBasic.cshtml*文件中的代码显示图表图像。</span><span class="sxs-lookup"><span data-stu-id="65b55-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="65b55-230">设置图表的样式</span><span class="sxs-lookup"><span data-stu-id="65b55-230">Styling a Chart</span></span>

<span data-ttu-id="65b55-231">`Chart`帮助程序支持大量选项，这些选项允许您自定义图表的外观。</span><span class="sxs-lookup"><span data-stu-id="65b55-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="65b55-232">您可以设置颜色、字体、边框等。</span><span class="sxs-lookup"><span data-stu-id="65b55-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="65b55-233">自定义图表外观的一种简单方法是使用*主题*。</span><span class="sxs-lookup"><span data-stu-id="65b55-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="65b55-234">主题是信息的集合，用于指定如何使用字体、颜色、标签、调色板、边框和效果来呈现图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="65b55-235">（请注意，图表的样式并不指示图表的类型。</span><span class="sxs-lookup"><span data-stu-id="65b55-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="65b55-236">下表列出了内置主题。</span><span class="sxs-lookup"><span data-stu-id="65b55-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="65b55-237">主题</span><span class="sxs-lookup"><span data-stu-id="65b55-237">Theme</span></span> | <span data-ttu-id="65b55-238">描述</span><span class="sxs-lookup"><span data-stu-id="65b55-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="65b55-239">在白色背景上显示红色列。</span><span class="sxs-lookup"><span data-stu-id="65b55-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="65b55-240">在蓝色渐变背景上显示蓝色列。</span><span class="sxs-lookup"><span data-stu-id="65b55-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="65b55-241">在绿色渐变背景上显示蓝色列。</span><span class="sxs-lookup"><span data-stu-id="65b55-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="65b55-242">在黄色渐变背景上显示橙色列。</span><span class="sxs-lookup"><span data-stu-id="65b55-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="65b55-243">在白色背景上显示三维红色列。</span><span class="sxs-lookup"><span data-stu-id="65b55-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="65b55-244">您可以指定创建新图表时要使用的主题。</span><span class="sxs-lookup"><span data-stu-id="65b55-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="65b55-245">创建名为*ChartStyleGreen.cshtml*的新文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="65b55-246">将页面中的现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="65b55-247">此代码与使用数据库进行数据的早期示例相同，但在创建`theme``Chart`对象时添加参数。</span><span class="sxs-lookup"><span data-stu-id="65b55-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="65b55-248">下面显示了更改的代码：</span><span class="sxs-lookup"><span data-stu-id="65b55-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="65b55-249">在浏览器中运行页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-249">Run the page in a browser.</span></span> <span data-ttu-id="65b55-250">您看到的数据与以前相同，但图表看起来更精美：</span><span class="sxs-lookup"><span data-stu-id="65b55-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="65b55-251">保存图表</span><span class="sxs-lookup"><span data-stu-id="65b55-251">Saving a Chart</span></span>

<span data-ttu-id="65b55-252">当您在本文中看到的帮助`Chart`器时，帮助程序每次调用图表时都会从头开始重新创建图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="65b55-253">如有必要，图表的代码还会重新查询数据库或重新读取 XML 文件以获取数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="65b55-254">在某些情况下，执行此操作可能是一个复杂的操作，例如，如果您要查询的数据库很大，或者 XML 文件包含大量数据。</span><span class="sxs-lookup"><span data-stu-id="65b55-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="65b55-255">即使图表不涉及大量数据，动态创建图像的过程也会占用服务器资源，如果许多人请求显示图表的页面或页面，也会对网站的性能产生影响。</span><span class="sxs-lookup"><span data-stu-id="65b55-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="65b55-256">为了帮助减少创建图表的潜在性能影响，可以在第一次创建图表时创建图表，然后保存它。</span><span class="sxs-lookup"><span data-stu-id="65b55-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="65b55-257">当再次需要图表时，而不是重新生成它时，只需获取保存的版本并呈现它。</span><span class="sxs-lookup"><span data-stu-id="65b55-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="65b55-258">您可以通过以下方式保存图表：</span><span class="sxs-lookup"><span data-stu-id="65b55-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="65b55-259">将图表缓存在计算机内存中（在服务器上）。</span><span class="sxs-lookup"><span data-stu-id="65b55-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="65b55-260">将图表另存为图像文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="65b55-261">将图表另存为 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-261">Save the chart as an XML file.</span></span> <span data-ttu-id="65b55-262">此选项允许您在保存图表之前对其进行修改。</span><span class="sxs-lookup"><span data-stu-id="65b55-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="65b55-263">缓存图表</span><span class="sxs-lookup"><span data-stu-id="65b55-263">Caching a Chart</span></span>

<span data-ttu-id="65b55-264">创建图表后，可以缓存它。</span><span class="sxs-lookup"><span data-stu-id="65b55-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="65b55-265">缓存图表意味着，如果需要再次显示图表，则不必重新创建图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="65b55-266">在缓存中保存图表时，会为它提供一个必须是唯一的图表的键。</span><span class="sxs-lookup"><span data-stu-id="65b55-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="65b55-267">如果服务器内存不足，则保存到缓存的图表可能会被删除。</span><span class="sxs-lookup"><span data-stu-id="65b55-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="65b55-268">此外，如果应用程序出于任何原因重新启动，缓存将被清除。</span><span class="sxs-lookup"><span data-stu-id="65b55-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="65b55-269">因此，使用缓存图表的标准方法是始终检查缓存中是否可用，如果没有，则先创建或重新创建它。</span><span class="sxs-lookup"><span data-stu-id="65b55-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="65b55-270">在网站的根目录，创建一个名为*ShowCachedChart.cshtml*的文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="65b55-271">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="65b55-272">该`<img>`标记包括指向`src` *ChartSaveToCache.cshtml*文件并将密钥作为查询字符串传递到页面的属性。</span><span class="sxs-lookup"><span data-stu-id="65b55-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="65b55-273">该键包含"我的图表&quot;键&quot;"的值。</span><span class="sxs-lookup"><span data-stu-id="65b55-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="65b55-274">*ChartSaveToCache.cshtml*文件包含创建图表`Chart`的帮助程序。</span><span class="sxs-lookup"><span data-stu-id="65b55-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="65b55-275">您将在一瞬间创建此页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="65b55-276">在页面的末尾有一个指向*名为ClearCache.cshtml*的页面的链接。</span><span class="sxs-lookup"><span data-stu-id="65b55-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="65b55-277">稍后还将创建该页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="65b55-278">您只需要*ClearCache.cshtml*来测试此示例的缓存 - 它不是在使用缓存的图表时通常包含的链接或页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="65b55-279">在网站的根目录，创建一个名为*ChartSaveToCache.cshtml*的新文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="65b55-280">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="65b55-281">代码首先检查是否传递了任何内容作为查询字符串中的键值。</span><span class="sxs-lookup"><span data-stu-id="65b55-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="65b55-282">如果是这样，代码会尝试通过调用`GetFromCache`方法并传递密钥来从缓存中读取图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="65b55-283">如果事实证明该键下的缓存中没有任何内容（在首次请求图表时发生），则代码将像往常一样创建图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="65b55-284">图表完成后，代码通过调用`SaveToCache`将其保存到缓存中。</span><span class="sxs-lookup"><span data-stu-id="65b55-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="65b55-285">该方法需要一个键（以便以后可以请求图表），以及图表应保存在缓存中的时间量。</span><span class="sxs-lookup"><span data-stu-id="65b55-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="65b55-286">（缓存图表的确切时间取决于您认为图表表示的数据可能会更改的频率。该方法`SaveToCache`还需要一个`slidingExpiration`参数&#8212;如果设置为 true，则每次访问图表时都会重置超时计数器。</span><span class="sxs-lookup"><span data-stu-id="65b55-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="65b55-287">在这种情况下，它实际上意味着图表的缓存条目在上次有人访问图表后 2 分钟过期。</span><span class="sxs-lookup"><span data-stu-id="65b55-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="65b55-288">（滑动过期的替代方法是绝对过期的，这意味着缓存条目在放入缓存后正好 2 分钟后过期，无论访问频率如何。</span><span class="sxs-lookup"><span data-stu-id="65b55-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="65b55-289">最后，代码使用 方法`WriteFromCache`从缓存中获取和呈现图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="65b55-290">请注意，此方法位于检查缓存`if`的块之外，因为它将从缓存中获取图表，无论图表是开始还是必须生成并保存在缓存中。</span><span class="sxs-lookup"><span data-stu-id="65b55-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="65b55-291">请注意，在此示例中，`AddTitle`该方法包含时间戳。</span><span class="sxs-lookup"><span data-stu-id="65b55-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="65b55-292">（它将当前日期和时间&#8212;&#8212;`DateTime.Now`添加到标题中。</span><span class="sxs-lookup"><span data-stu-id="65b55-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="65b55-293">创建名为*ClearCache.cshtml*的新页面，并将其内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="65b55-294">此页使用`WebCache`帮助程序删除在*ChartSaveToCache.cshtml*中缓存的图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="65b55-295">如前所述，您通常不必有这样的页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="65b55-296">您在此处创建它只是为了更轻松地测试缓存。</span><span class="sxs-lookup"><span data-stu-id="65b55-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="65b55-297">在浏览器中运行*ShowCachedChart.cshtml*网页。</span><span class="sxs-lookup"><span data-stu-id="65b55-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="65b55-298">该页面基于*ChartSaveToCache.cshtml*文件中的代码显示图表图像。</span><span class="sxs-lookup"><span data-stu-id="65b55-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="65b55-299">记下时间戳在图表标题中的内容。</span><span class="sxs-lookup"><span data-stu-id="65b55-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![说明：图表标题中带有时间戳的基本图表图片](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="65b55-301">关闭浏览器。</span><span class="sxs-lookup"><span data-stu-id="65b55-301">Close the browser.</span></span>
8. <span data-ttu-id="65b55-302">再次运行*ShowCachedChart.cshtml。*</span><span class="sxs-lookup"><span data-stu-id="65b55-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="65b55-303">请注意，时间戳与以前相同，表示图表未重新生成，而是从缓存中读取。</span><span class="sxs-lookup"><span data-stu-id="65b55-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="65b55-304">在*ShowCachedChart.cshtml*中，单击**清除缓存**链接。</span><span class="sxs-lookup"><span data-stu-id="65b55-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="65b55-305">这将带您到*ClearCache.cshtml*，它报告缓存已清除。</span><span class="sxs-lookup"><span data-stu-id="65b55-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="65b55-306">单击 **"返回显示缓存图表.cshtml"** 链接，或从 WebMatrix 重新运行*ShowCachedChart.cshtml。*</span><span class="sxs-lookup"><span data-stu-id="65b55-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="65b55-307">请注意，此时间戳已更改，因为缓存已清除。</span><span class="sxs-lookup"><span data-stu-id="65b55-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="65b55-308">因此，代码必须重新生成图表并将其放回缓存中。</span><span class="sxs-lookup"><span data-stu-id="65b55-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="65b55-309">将图表保存为图像文件</span><span class="sxs-lookup"><span data-stu-id="65b55-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="65b55-310">您还可以将图表另存为服务器上的图像文件（例如，作为 *.jpg*文件）。</span><span class="sxs-lookup"><span data-stu-id="65b55-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="65b55-311">然后，您可以像使用任何图像的方式使用图像文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="65b55-312">优点是文件被存储而不是保存到临时缓存。</span><span class="sxs-lookup"><span data-stu-id="65b55-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="65b55-313">您可以在不同时间（例如，每小时）保存新的图表图像，然后保留随时间变化的永久记录。</span><span class="sxs-lookup"><span data-stu-id="65b55-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="65b55-314">请注意，必须确保 Web 应用程序具有将文件保存到要放置图像文件的服务器上的文件夹的权限。</span><span class="sxs-lookup"><span data-stu-id="65b55-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="65b55-315">在网站的根目录，创建名为*\_ChartFiles 的*文件夹（如果该文件夹不存在）。</span><span class="sxs-lookup"><span data-stu-id="65b55-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="65b55-316">在网站的根目录，创建一个名为*ChartSave.cshtml*的新文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="65b55-317">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="65b55-318">代码首先通过调用`File.Exists`方法检查 *.jpg*文件是否存在。</span><span class="sxs-lookup"><span data-stu-id="65b55-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="65b55-319">如果文件不存在，代码将从数组创建一个新`Chart`。</span><span class="sxs-lookup"><span data-stu-id="65b55-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="65b55-320">这一次，代码调用`Save`方法并传递`path`参数以指定保存图表的位置的文件路径和文件名。</span><span class="sxs-lookup"><span data-stu-id="65b55-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="65b55-321">在页面正文中，`<img>`元素使用路径指向要显示的 *.jpg*文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="65b55-322">运行*ChartSave.cshtml*文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="65b55-323">返回到 WebMatrix。</span><span class="sxs-lookup"><span data-stu-id="65b55-323">Return to WebMatrix.</span></span> <span data-ttu-id="65b55-324">请注意，名为*chart01.jpg*的图像文件已保存在*\_图表文件*文件夹中。</span><span class="sxs-lookup"><span data-stu-id="65b55-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="65b55-325">将图表保存为 XML 文件</span><span class="sxs-lookup"><span data-stu-id="65b55-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="65b55-326">最后，您可以将图表另存为服务器上的 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="65b55-327">使用此方法对缓存图表或将图表保存到文件中的一个优点是，如果需要，可以在显示图表之前修改 XML。</span><span class="sxs-lookup"><span data-stu-id="65b55-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="65b55-328">应用程序必须具有要放置映像文件的服务器上的文件夹的读/写权限。</span><span class="sxs-lookup"><span data-stu-id="65b55-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="65b55-329">在网站的根目录，创建一个名为*ChartSaveXml.cshtml*的新文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="65b55-330">将现有内容替换为以下内容：</span><span class="sxs-lookup"><span data-stu-id="65b55-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="65b55-331">此代码类似于您之前在缓存中存储图表时看到的代码，只不过它使用 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="65b55-332">代码首先通过调用`File.Exists`方法检查 XML 文件是否存在。</span><span class="sxs-lookup"><span data-stu-id="65b55-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="65b55-333">如果文件确实存在，代码将创建一个新`Chart`对象，并将文件名作为`themePath`参数传递。</span><span class="sxs-lookup"><span data-stu-id="65b55-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="65b55-334">这将基于 XML 文件中的任何内容创建图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="65b55-335">如果 XML 文件不存在，代码将创建一个类似于正常图表的图表，然后调用`SaveXml`以保存它。</span><span class="sxs-lookup"><span data-stu-id="65b55-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="65b55-336">图表使用 方法呈现，`Write`如您以前看到的那样。</span><span class="sxs-lookup"><span data-stu-id="65b55-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="65b55-337">与显示缓存的页面一样，此代码在图表标题中包括时间戳。</span><span class="sxs-lookup"><span data-stu-id="65b55-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="65b55-338">创建名为*ChartDisplayXMLChart.cshtml*的新页面，并添加以下标记：</span><span class="sxs-lookup"><span data-stu-id="65b55-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="65b55-339">运行*图表显示XMLChart.cshtml*页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="65b55-340">将显示图表。</span><span class="sxs-lookup"><span data-stu-id="65b55-340">The chart is displayed.</span></span> <span data-ttu-id="65b55-341">记下图表标题中的时间戳。</span><span class="sxs-lookup"><span data-stu-id="65b55-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="65b55-342">关闭浏览器。</span><span class="sxs-lookup"><span data-stu-id="65b55-342">Close the browser.</span></span>
6. <span data-ttu-id="65b55-343">在 WebMatrix 中，右键单击*\_图表文件*文件夹，单击 **"刷新**"，然后打开该文件夹。</span><span class="sxs-lookup"><span data-stu-id="65b55-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="65b55-344">此文件夹中的*XMLChart.xml*文件由`Chart`帮助程序创建。</span><span class="sxs-lookup"><span data-stu-id="65b55-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![说明：显示图表帮助程序创建的 XMLChart.xml 文件的_ChartFiles文件夹。](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="65b55-346">再次运行*图表显示XMLChart.cshtml*页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="65b55-347">该图表显示的时间戳与首次运行页面时的时间戳相同。</span><span class="sxs-lookup"><span data-stu-id="65b55-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="65b55-348">这是因为图表是从您之前保存的 XML 生成的。</span><span class="sxs-lookup"><span data-stu-id="65b55-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="65b55-349">在 WebMatrix 中，打开*\_图表文件*文件夹并删除*XMLChart.xml*文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="65b55-350">再次运行*图表显示XMLChart.cshtml*页面。</span><span class="sxs-lookup"><span data-stu-id="65b55-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="65b55-351">这一次，时间戳将更新，因为`Chart`帮助程序必须重新创建 XML 文件。</span><span class="sxs-lookup"><span data-stu-id="65b55-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="65b55-352">如果需要，请检查*\_ChartFiles*文件夹，并注意到 XML 文件已返回。</span><span class="sxs-lookup"><span data-stu-id="65b55-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="65b55-353">其他资源</span><span class="sxs-lookup"><span data-stu-id="65b55-353">Additional Resources</span></span>

- [<span data-ttu-id="65b55-354">ASP.NET网页网站中使用数据库的简介</span><span class="sxs-lookup"><span data-stu-id="65b55-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="65b55-355">在ASP.NET网页网站中使用缓存以提高性能</span><span class="sxs-lookup"><span data-stu-id="65b55-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="65b55-356">[图表类](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99))（ASP.NET MSDN 上的网页 API 引用）</span><span class="sxs-lookup"><span data-stu-id="65b55-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
